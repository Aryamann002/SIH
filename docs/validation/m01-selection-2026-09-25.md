# M01 rich-manifest selector checkpoint — 2026-09-25

Status: **implementation checked; representative evaluation not run**.

The prior rich evaluator chose a HIGH threshold by minimum balanced error on a 0.01 grid, contradicting the [pre-frozen protocol](../evaluation-protocol.md). The rich validation path now checks observed-score cutoffs and chooses the highest synthetic recall subject to at most 10% HIGH blocks among scorable genuine validation clips. Ties favor fewer genuine blocks, then a higher threshold. If no feasible cutoff exists, it fails instead of silently relaxing the safety bound. The five-column historical exploratory path retains its old selector and result semantics. Validation still performs no test inference; final-test mode requires a supplied frozen threshold. Reports add candidate per-language/generator/channel/replay/attack slices beside the unchanged deployed-risk slices.

The manifest validator now accepts validation/test-only Stage 0 manifests; train requires both classes only if a train row is present. A canonical speaker ID appearing as the heard voice, conversion source or conversion target cannot cross splits. It still cannot prove hidden cross-dataset ancestry or establish independent speakers when the source metadata are wrong.

Focused evaluator/manifest tests: **5 passed**. Full local Python: **34 passed, 1 skipped, 26 subtests**. UI and microphone Node smoke suites passed; `git diff --check` exited 0 with line-ending warnings. Red tests first failed on unsupported `max_fpr` and mandatory train rows, then passed after the change. The exact-source isolated image `sha256:7e05b4b9ca2322786c1e9fdbbfd707b1eacaf9503b76f4c2783812deb3a6eb52` matched `scripts/evaluate_audio.py` SHA256 `9f38828f22e1aed6833137ee51b6f167d1161041f360a0ce474f6fdb1ce1e885` and `scripts/validate_manifest.py` SHA256 `56e631ebad78cbfb990f16064f3eac166e5ed4d1523a26774ba6c4c7d3dbe07e`.

No rights-cleared, speaker/source/generator-disjoint four-language corpus was obtained or scored. This does **not** validate the current detector, calibrate scores, select a deployed threshold, or permit a final-test run. Duration, partial-spoof, confidence intervals and first-alert slice reporting remain incomplete.
