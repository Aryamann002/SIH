# S03/V01 browser-to-action receipt — 2026-09-24

HEAD `58f7e32` plus a dirty worktree is not an immutable source identity. The final isolated Python 3.11 backend image after the V02 upload-admission fix was `sha256:136591533727ec788a0e6365adf395ceefe562c3e856637b8ec36efa0f51c2d4`; it used the separate `vv-s02-db` test database, not the existing demo stack. Application hashes: `sessions.py` `1ad24d0794e337396a70b41f83bca50de3f2ffb8baab117ffe9d032fff401521`, `stream.py` `edd1c00c81e7434ef17965b19ac2a29f938706aa63d7847b881c72131e47c8c7`, `action_gate.py` `22bb1d1b5e5da0bf60f9f8518bf23881fca20efce2609d75d4b35e996addf579`, `app.js` `cecdc1cbc676cbacede0a9f5a01b8fdadd40f86bea0f58bc19fcd458b2276aef`, `index.html` `048a911be1a9db5cbf3f47cb6ed4481d16b9e3723e957ba9fdd6b31fe326bbb4`. Browser harness hash: `check_browser.py` `74361f9788aaa1534a6d9dda4be5485e85d07e562116cd0eaa525ab848b8e611`.

The session GET route now reports server-owned evidence age. The dashboard clears stale risk on stream stop, refreshes risk/action status while live or an action exists, displays reason codes and age, and keeps completion controls unavailable on unsafe evidence. Stream admission now checks capacity before changing a same-session audio generation, so a rejected second stream cannot disrupt the admitted one. No protected-action contract was loosened.

| Check | Result |
|---|---|
| `python -m pytest -q -p no:cacheprovider` | 30 passed, 1 skipped, 26 subtests |
| `node tests/ui_smoke.cjs`; `node tests/microphone_smoke.cjs` | Passed, including pagehide capture release and permission denial |
| `docker exec -w /app vv-v03-backend python scripts/check_demo.py --output /tmp/v03-final-checks.json` | 20/20 on the final image, including same-session stream/WAV overload without source replacement, HIGH retention, OTP/ownership/audit and fail-closed paths |
| `python scripts/check_browser.py --base-url http://127.0.0.1:<isolated-port>/ --action-flow --output docs/validation/s03-action-browser-2026-09-24` | [Browser report](s03-action-browser-2026-09-24/browser-checks.json): 15 checks passed, no JS exceptions; fake-device live risk, stop/reconnect, WAV fallback, genuine verification/completion, synthetic HIGH/block, mobile layout |
| Browser live transition | A VERIFIED action was blocked by synthetic HIGH substituted into the *same sequenced live stream*; direct completion returned HTTP 409; later LOW did not revive it. The persisted action-block audit was visible in the browser. |
| Browser source replacement | A pending action was blocked and shown as blocked after a synthetic WAV replaced its audio source. A new action on synthetic HIGH was blocked. |
| `/readyz`; `git diff --check`; `python -m py_compile scripts/check_browser.py` | Ready 200; syntax and whitespace checks passed (Git reported only expected LF/CRLF conversion warnings). |

The first added browser transition attempt created an action before the new live stream produced evidence; the hard gate correctly blocked it. A second harness attempt clicked verification before the two-second UI refresh enabled it. The harness now waits for fresh live results and the enabled control; the final run passed. These were test-order races, not evidence of unsafe completion.

The first S03 image was `sha256:c37fa553804968ac30f142ff083939406764ef682215983c697ee543d3a6c54e`. The final image above was rebuilt after a separate V02 regression found that a rejected overlapping WAV upload could replace an admitted live generation. On the first image the new test failed at `after == before`; on the final image 20/20 API groups and the full browser flow passed again. The browser report was overwritten by this final-image rerun at `2026-09-24T05:16:21Z`.

The browser uses Chrome's file-backed fake microphone, not a physical device or OS permission dialog. Permission denial/pagehide are VM checks. The synthetic-frame substitution is test-only; it preserves the live socket, sequence numbers and cadence but is not an acoustic replay through laptop speakers. No production banking action occurred. The original R01 archives predate these S03 source changes; `v03` replacements have since been made, but final package validation remains open. No verifier key, OTP, private audio or approval token is in the receipt.

Focused security review after the `v03` rebuild found the changed stream/upload routes authenticate before capacity admission and recheck ownership with a row lock before `begin_audio`; rejected overload no longer changes the active generation. The existing action completion path still rechecks ownership, evidence, single-use approval and audit in one database transaction. The Jev path sends only structured buckets and cannot select a completion response. This is source/test review, not a complete dependency-vulnerability scan: the local Ruflo security-scan CLI was not installed (`npx --no-install` refused to fetch it). A proposed isolated database-outage command was rejected by the command runner before execution, so DB-outage completion behavior remains unverified. The later `v03` package archives are linked from the [updated offline manifest](offline-package-2026-09-24.md); the older unsuffixed archives remain superseded.
