# B03 readiness receipt

Generated: 2026-09-13 13:27 Asia/Calcutta

## Source and runtime

- Baseline commit: `cb38a5d46764a8ae0bbc7e885bbd5f154df4adbe` (the externally integrated B01/B02/D01 state present before the final B03 receipt)
- B03 tracked patch Git object: `e1b5816a13b14d4ec9299358edfcc9fee8a5d94e`
- `tests/test_readiness.py` Git object: `f558fe9f69d230ec4fda8107f465a1e650ea79dd`
- Rebuilt backend image: `sha256:dce04a2102c7a33c776ce010924e6fa05ae8273c1372c43dff68e61e04b3e106`
- Verified runtime: Python 3.11.16 in Docker. The host was repaired from incompatible SQLAlchemy 2.0.30 to 2.0.52 on Python 3.13.5.

## Implemented

- Kept `/healthz` as cheap liveness and added `/readyz` with HTTP 200/503 aggregate readiness.
- `/api/v1/system` and the browser now require database connectivity, every runtime-referenced migrated schema column, executable Silero VAD, executable spoof detector, and configured local verifier.
- Startup performs one bounded, cached VAD inference and one bounded, cached spoof-detector inference. Failure leaves the service live but unready.
- Settings reject non-finite/reversed thresholds, evidence duration outside the audio window, invalid EMA/VAD/RMS/window bounds, nonpositive inference/OTP/approval/evidence durations, audio sizes outside the supported WAV cap, and an OTP attempt limit other than three.

## Checks

- `python -m pytest -q -p no:cacheprovider`: 20 passed, 1 optional model test skipped, 22 validation subtests passed in 0.86 seconds; one unrelated `python_multipart` pending-deprecation warning.
- Python 3.11 focused unittest: 8 passed in 0.119 seconds.
- UI and microphone Node smoke checks: passed.
- `docker compose up -d --build`: passed; existing PostgreSQL container/volume preserved.
- Live `/healthz`: HTTP 200 liveness. Live `/readyz` and `/api/v1/system`: HTTP 200 with DB, schema, VAD, detector, and verifier all true.
- Missing verifier, missing spoof model, and unreachable database/schema processes each returned `ready:false`; no secret or model fallback score was exposed.
- `HIGH_RISK_SPOOF_THRESHOLD=nan` failed settings construction before startup.
- Final rebuilt-stack `scripts/check_demo.py`: all 13 groups passed; JSON SHA-256 `dc7f3f12e3513e01d3d75dfcfcffe820e981bb66a8501734395d3eecf3ea8dd1`.
- Final read-only review found no remaining blocking issue after schema and executable-warmup fixes.

## Limits

Readiness proves the configured local serving path at probe time; it is not detector accuracy, sustained-capacity, restart-recovery, or production availability evidence. Cached unavailable model status requires a process restart after changing model files, consistent with the model runbook.
