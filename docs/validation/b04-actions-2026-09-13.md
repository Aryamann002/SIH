# B04 protected-action failure and restart receipt

Date: 2026-09-13 (Asia/Calcutta)

Result: **PASS**. All prohibited completion attempts were denied; legitimate actions completed exactly once.

## Source and runtime

- Baseline commit: `5a5e62b` (`B03 complete`); B04 remains an uncommitted reviewable patch.
- Backend image: `sha256:bca29d6d58c1cf0537bb8230f5bbfaeb90855c168e69185ae59e883f75a1cc4e`.
- Runtime: Python 3.11.16, FastAPI 0.141.1, SQLAlchemy 2.0.52, asyncpg 0.31.0, ONNX Runtime 1.30.0.
- Models/profile: `wav2vec2-xlsr-int8-4b1c4a294ab6` SHA256 `7af799e8443c3d030bb3ef644ffc6e74f5b6fddcd6a4286ee9abe9e828cb762e`; `silero-v5.1` SHA256 `2623a2953f6ff3d2c1e61740c6cdb7168133479b267dfef114a4a3cc5bdd788f`; `prototype-uncalibrated-v1`.
- Changed Git object hashes: `app/core/database.py` `d3056d0c5b5056154d1cb0eb0903211dbebaf5af`; `app/main.py` `5303b5a0dbb3dc2fdbe78c5476ac813345e07208`; `scripts/check_demo.py` `69f1470d6ba1eb9a8c5bb118ab2402f3cb35537e`; `tests/test_readiness.py` `e3020b062c894c20cc01e4b3330d261abc2c8b38`.
- Existing PostgreSQL container and volume were preserved. Every action/session created by the checks used fresh identifiers.

## Change and risk closure

- Startup now atomically invalidates persisted `LIVE`/`PROCESSING_FILE` audio state and records `AUDIO_UNAVAILABLE` with `BACKEND_RESTART`. This closes the window where fresh persisted live evidence could authorize an action after its WebSocket process died.
- The existing API harness injects an audit-write failure after the approval/action updates but before commit, then proves the action remains `VERIFIED`, the approval remains unconsumed, no completion event exists, and the same approval succeeds once audit is restored.
- A separate one-off tester container kept a live WebSocket active while `docker stop --signal SIGKILL vigilvoice_backend` abruptly terminated only the backend, followed by `docker start vigilvoice_backend`. Verification required exactly one startup-only `AUDIO_UNAVAILABLE / SERVICE_UNAVAILABLE / BACKEND_RESTART` audit row. The private fixture was mode `0600`, contained client credentials but no OTP, remained only in tester `/tmp`, and was deleted before that temporary container was removed.

## Checks

- Python syntax plus `git diff --check`: passed.
- Python 3.11 focused unit checks: 15 passed, 1 skipped (`test_audio.py`, `test_evaluation.py`, `test_readiness.py`).
- `docker exec vigilvoice_backend python scripts/check_demo.py --output /tmp/b04-demo-checks.json`: 14/14 groups passed, including ownership, action/payload binding, replay/race denial and injected audit rollback. [Machine receipt](b04-demo-checks-2026-09-13.json), SHA256 `a7c1a04b7ebc0eaf4f0828eac916d87a482562c97ec5540e3714efe4d0d68188`.
- Restart sequence: separate `docker compose run` tester preparation, `docker stop --signal SIGKILL vigilvoice_backend`, `docker start vigilvoice_backend`, readiness poll, then `--restart-verify`; 5/5 groups passed—completed action/audit persisted exactly once, the startup audit marker proved live-evidence invalidation, expired approval stayed denied, lost verifier inbox could not bypass the pending challenge, and a fresh action recovered legitimately. [Machine receipt](b04-restart-checks-2026-09-13.json), SHA256 `fde260b3b52f6ff532869378f0adffb8080e8b5cce566a5842fa633b508e9d9e`.
- UI and microphone smoke checks: passed.
- Final stack: backend running and PostgreSQL healthy; `/readyz` returned 200.

The broad container test-discovery command could not load `pytest` because the runtime image intentionally does not include it; all runnable unittest files affected by this change were then executed explicitly. Dependency/test packaging remains R01 scope.

## Ruflo coordination

Ruflo v3.35.0 passed structural diagnostics. Memory store/retrieve, semantic routing, and hierarchical swarm `swarm-1789287394759-rzsgso` were verified before implementation. Optional API-key, AIDefence, TypeScript, agentic-flow and MetaHarness warnings were not installed because B04 did not require them.
