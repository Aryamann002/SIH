# R01 packaging checkpoint — 2026-09-24

This is a checkpoint, not the final release package. HEAD `58f7e32` is dirty and is not an immutable source identity. The new `.dockerignore` admits only `app/`, `scripts/`, `config/`, `init.sql`, `requirements.txt` and `Dockerfile` into the build context, excluding local models, research PDFs, `.env`, runtime data and evaluation outputs. `Dockerfile` pins the tested Python 3.11 base-image digest and removes the unnecessary compiler install. `requirements.txt` pins the 28 packages reported by `pip freeze` in the earlier passing Python 3.11 validation image. File SHA256: `.dockerignore` `2fcf88ce2597f7cea8499594f845967a5934c3f7d5b364a18aad6af6081ac096`, `Dockerfile` `8bb3c3926a76bb5864292aa7bfc6466b2499282da1e352baef0502cc4ad931f6`, `requirements.txt` `599379992d106c20ef3a3f7cf671493b7bf4060bc44a1493e6563100d39a1670`. Built image: `sha256:2a0331f4a8fbbd8d71fc0d7d8cb30e72cb09fcbe7710c6bdd0e222287d0c2ced`.

Checks on the isolated test database and model bind mount:

| Command/check | Result |
|---|---|
| `docker build -t vigilvoice-validation:r01 .` | Passed from the narrowed context without a compiler layer. Build fetched the pinned wheels over the Internet; this does not prove offline rebuilding. |
| Image `import app.main`, `import scripts.check_demo`; `python -m pip check` | Passed; no broken installed requirements. |
| Enumerate `/app` in image | Only `app`, `config`, `init.sql`, `requirements.txt`, `scripts`; no `.env`, private key, PDF or WAV found. |
| New `vv-r01-backend` on `vv-s02-net`, local model mount and test DB | `/readyz` 200 after startup; existing `check_demo.py` passed 19/19 live action/stream checks. |
| New `vv-r01-offline` on Docker `--internal` network with the same local DB and models, `JEV_MODE=shadow` without a key | `/readyz` 200; 19/19 live checks passed in 35.966 s. TCP connect to `1.1.1.1:443` returned network-unreachable (`101`). This verifies the local workflow without Internet egress, not an offline Docker image build. |

The existing demo stack and its PostgreSQL volume were not changed. The first R01-only test containers and internal network were removed after checks; the isolated S02 test database and stack remain. A 42-file runtime-source ZIP from the dirty worktree was extracted and built; both images contained 40 identical `/app` files with a combined SHA256 `bb98d4a2ee471c355031dbec6b1878c8dfe3221020b1be68fc964fdcef03cb8e`. Their image IDs differed, so filesystem equivalence—not byte-identical image identity—is claimed. The extracted build passed `/readyz` and 5/5 hard-SIGKILL restart checks in 2.102 s against the preserved isolated test database. The source ZIP included untracked `jev.py`, the manifest validator and the Jev evaluator.

An [offline package checkpoint](offline-package-2026-09-24.md) now binds a 44-file source ZIP, backend and PostgreSQL image archives, and a model/public-demo archive by SHA256. Both images loaded into the same daemon with expected IDs; the extracted source Compose file selected their tags, and extracted model/demo hashes matched `models/manifest.json`. The superseded test-tag backend tar was removed after the `vigilvoice:local` archive was verified. Large image/model archives are local ignored files, not Git artifacts.

R01 remains IN_PROGRESS: this is not a final source freeze or a pristine-host Compose run, and no upgrade rehearsal against the existing demo database was performed. Exact package versions and base digest are pinned, but wheel hashes are not; rebuilding still needs a package index unless wheels are preloaded. Runtime requires the verified local model files. Do not call this an immutable release or minimum-hardware proof.

## September 24 v03 checkpoint after S03/V02

The first source/backend archives above were superseded after the browser and same-session WAV admission fixes. The [updated offline manifest](offline-package-2026-09-24.md) identifies new `v03` source/backend archives and hashes while reusing the unchanged PostgreSQL and model/public-demo archives. The `v03` backend image ID is `sha256:136591533727ec788a0e6365adf395ceefe562c3e856637b8ec36efa0f51c2d4`. The source ZIP was staged from that image's exact 40 `/app` files plus Dockerfile, `.dockerignore`, Compose and README; extraction matched all 44 file hashes and contained no secret, audio, model or PDF. Compose config validated with placeholder credentials, without creating a `.env`. The backend tar loaded into the same Docker daemon at the expected image ID.

On a temporary Docker `--internal` network, the exact `v03` image reached `/readyz` 200, rejected external TCP with `ENETUNREACH` 101, and passed the updated 20/20 API/action suite in 40.385 seconds with the isolated local database and read-only models. The temporary container and internal network were removed; original demo containers/volume and the test database were retained. The [S03/V01 receipt](s03-2026-09-24.md) and [V03 runtime checkpoint](v03-2026-09-24.md) cover the final-image browser, ten-minute stream and hard restart. R01 still lacks a *pristine-host* Compose launch, a final-source freeze and a complete bundled documentation tree. The current archives are a local checkpoint, not release artifacts.

## September 25 source-and-documentation checkpoint

The earlier source ZIP omitted `docs/`, leaving ten local README documentation links broken after extraction. `python -m scripts.package_offline --output <new-zip-path>` now writes a deterministic source-and-docs ZIP from an allowlist: root launch files, application/scripts/configuration source, and documentation, including the presentation PDFs and the explicitly historical silent video. It rejects missing/unsafe paths and missing local README links; it excludes `.env`, model weights, WAV recordings, old ZIP/tar artifacts and preview directories, and refuses to overwrite an existing ZIP. Models and Docker images remain separate artifacts. This is a packaging aid, not a content-security scanner or release-signing mechanism.

The focused packager test passed; full local checks passed **35 Python tests, 1 skipped, 26 subtests**, plus both UI and microphone Node smoke suites. A separate current-source image `vigilvoice-validation:2026-09-25` (`sha256:5f79f4720ff12045116e8abde9c3124390c57854c48227be60e4d913eca759ed`) used the existing isolated `vv-s02-db` and read-only model mount; the API harness wrote **22/22 passing groups** and exited 0, and all three post-COMMIT ambiguity checks passed. The first API attempt used `JEV_MODE=disabled` and failed only because the harness explicitly asserts the default shadow audit; it was rerun with shadow/no key. No presentation database or model/policy configuration was changed. The archive generated after the plan handoff is a checkpoint only; record its SHA256 externally because embedding that hash in a bundled document would make the ZIP self-referential.

R01 remains **IN_PROGRESS**. A complete offline release still needs the model/policy decision, final exact-source backend and PostgreSQL/model archives, a pristine-host Compose launch, preserved-database rehearsal, and physical/offline presentation checks. This checkpoint does not make the old `v03` archives current.

## September 25 current-build no-egress check

The API harness previously required a `shadow`/`missing_key` audit and could not exercise the recommended `JEV_MODE=disabled` offline configuration. It now asserts the configured `shadow` or `disabled` mode and corresponding fallback without relaxing the deterministic action assertions. Focused Jev/packager tests passed **3/3**; the 20 scripted Jev scenarios still had zero fixture unsafe downgrades, but are not live service measurements.

Built `vigilvoice-validation:offline-current` (`sha256:eac8e0aebca3003aa8fce4f4e8910d4d9d9f663ac0cec51d72990a15a617c189`) from the changed source. Only the test database `vv-s02-db` was additionally attached to a new Docker `--internal` network; the backend had no other network, used local read-only models, and ran with `JEV_MODE=disabled`. `/readyz` returned ready, an attempted external TCP connection returned `ENETUNREACH` (`101`), and `scripts/check_demo.py` exited 0 with **22/22 passing groups** in 42.068 seconds. These include real local audio inference, action/OTP/audit checks, HIGH retention, stream faults and concurrent completion. No Jev key or Internet request was made. This is an isolated no-egress runtime check, **not** a pristine-host, physically disconnected Internet, or archive-loaded Compose rehearsal.

The same offline container processed twelve sequential repeated-public-clip 10-second PCM16 WAV uploads: median **5,216.10 ms**, nearest-rank p95 **5,341.72 ms**, all ELEVATED. One further upload returned ELEVATED while a single `docker stats` sample showed backend **297.19%** of one logical CPU and **825.5 MiB** memory, test PostgreSQL **59.53 MiB**. The input SHA256 was `0b73d2476d9cb2e4877f1c1ad345c461621c02e09d70f5f6a6cc1d3bae5ef344`, the same repeated public clip used by V03; these are timing/resource observations, not independent accuracy samples or hardware minimums. See the [V03 checkpoint](v03-2026-09-24.md). Generated local archives, extracted source and public WAV are ignored in `docs/validation/artifacts/` to prevent accidental commit.

An extracted ZIP was initially placed below `docs/validation/artifacts/`; unconstrained pytest discovery found its duplicate `scripts/test_client.py` and stopped at collection. `pytest.ini` now excludes that generated-artifact directory, and the full suite passed again with the extracted copy still present (**35 passed, 1 skipped, 26 subtests**). The failure was a validation-layout collision, not an application failure. Future pristine-host checks should extract outside the repository.

The next extraction under Windows `%TEMP%` exposed a short-path alias (`ARYAMA~1`) in the caller's root. The first `source_files()` comparison mixed that lexical path with a resolved long path and raised `ValueError`; the shell still built the image afterward, so that first extracted-source self-check was **not** a pass. The packager now resolves the root once before validating file paths and README links. A root-alias regression test and the exact extracted-folder check pass; final local regression after this fix is **36 passed, 1 skipped, 26 subtests**, with both Node smoke suites passing. No app runtime or policy changed. Generate the next checkpoint ZIP only after this fix and the plan handoff; earlier ZIPs in the artifact folder are superseded.

### Validated v3 checkpoint (not the final release)

The local ignored `docs/validation/artifacts/offline-source-with-docs-v3-2026-09-25.zip` contains 116 allowlisted files and has SHA256 `c196d5a5c47335b5cd3f7101c89835ec21d8a2bb92cf443d36ddd6929b287344`. `python -m zipfile -t` passed. Its extraction under `%TEMP%` passed the 116-file/README-link validator and `docker compose config --quiet` with test-only placeholders. Docker built `vigilvoice-validation:source-with-docs-v3` (`sha256:bbda6ca0699c4a88c762121627e3caf282941bea8b3ea4d132d08f5b8fc0359a`) from that extraction. The archive contains the source/docs state immediately before this receipt and the final plan handoff were appended; it is deliberately not claimed to equal the later working tree.

That extracted-source image used only the internal-only test network, `vv-s02-db`, a read-only `/app/models` mount and `JEV_MODE=disabled`. `/readyz` passed, external TCP returned `ENETUNREACH` (`101`), and `scripts/check_demo.py` exited 0 with **22/22 groups** in **40.667 s**. `scripts/check_commit_ambiguity.py` then passed the three committed-but-unreturned challenge, approval and completion cases. The temporary test backends and internal network were removed; `vv-s02-db` stayed running/unpaused on its original `vv-s02-net`, and the presentation containers/volume were untouched. This supports an offline-capable source ZIP, not a frozen model, final image archive, physical no-Internet rehearsal or pristine-host Compose launch. V02 still lacks a true wire-level COMMIT pause and frozen-release check.

### September 26 test-complete source ZIP checkpoint

The source ZIP previously omitted `tests/` and `pytest.ini` despite the packaged README's commands to run those tests. The allowlist now includes only `.py`, `.cjs` and `.json` test files plus `pytest.ini`; model weights, recordings and credentials remain excluded. The regression check failed before the fix and passed afterward. `python scripts/package_offline.py --output docs/validation/artifacts/offline-source-with-tests-2026-09-26.zip` produced an ignored 129-file checkpoint with SHA256 `b2c2c123088f2df65b355edad5db069df034bbba5f3b8f5cd77e11f11953ebdc`; `python -m zipfile -t` passed. Extracted outside the repository under `%TEMP%`, it ran `python -m pytest -q -p no:cacheprovider` (**36 passed, 1 skipped, 26 subtests**) and both Node smoke suites successfully. The same checks passed in the source checkout. The archive predates the later V02 script and model-gate receipt, so it is a reproducibility checkpoint, not the final exact-source release ZIP. No app runtime, model, threshold, presentation container or demo database changed for this packaging fix.

### September 26 extracted Compose/preserved-test-DB checkpoint

After the V02 and model-gate handoff, `python scripts/package_offline.py --output docs/validation/artifacts/offline-source-checkpoint-2026-09-26-1035.zip` produced an ignored **131-file** source/docs/test ZIP, SHA256 `6dc321fe14a3f34efd4c53c9f1fd46a41ee4e1df04df82347c8d2831e7331908`. ZIP integrity and extracted 131-file allowlist/README-link self-check passed. Extraction outside the repo ran Python **36 passed, 1 skipped, 26 subtests**, both Node smoke suites and `docker compose config --quiet`. The separate `runtime-models-2026-09-24.zip` was extracted beside it; both ONNX and both public-demo WAV hashes matched the workspace files. No private audio or credentials entered either archive.

`docker build -t vigilvoice-validation:sep26-checkpoint .` from that extraction produced image `sha256:c07e5a0212457bdc3b3677334da99e98bbc365f4de497fc4fa39ccbf5652b6f5`; a `--network none` import of `app.main` passed. A temporary override in the extracted folder named the test-only containers `vv-sep26-db` and `vv-sep26-backend` and selected this image. Using the existing local `.env` *outside* the ZIP for runtime credentials, `docker compose --env-file <local-env-path> -p vv-sep26 -f docker-compose.yml -f validation-compose.override.yml up -d --no-build --pull never` created a fresh `vv-sep26_postgres_data` volume and reached `/readyz` 200 after warmup. `docker exec -e VIGILVOICE_ISOLATED_TEST_DB=1 vv-sep26-backend python scripts/check_demo.py --output /tmp/sep26-fresh-compose-checks.json` exited 0 with **22/22** action, audio, stream and failure checks. The first immediate readiness request ended prematurely during warmup; the next request returned 200.

The test stack was stopped and restarted with the same Compose command and **without deleting its volume**. After warmup `/readyz` again returned 200; completed actions, `ACTION_COMPLETED` audits and total actions stayed **4 / 4 / 27**. The temporary stack was then stopped, not removed; its test volume remains, and the presentation containers/volume were untouched. This is a same-host fresh-volume and preserved-volume Compose test, **not** a pristine-host, no-Internet fresh launch, frozen release image or final matched offline image/model archive. The ZIP predates this receipt addition and remains a named checkpoint. No source commit or push.

After that test, the existing presentation containers were found stopped following a Docker restart. Starting their existing database/backend preserved **93 sessions, 333 actions, 58 completions and 4,455 audit rows** and restored `/readyz` 200. Inspection then found that the old running backend image lacked the current V02 database-timeout code. The validated extracted-source image `sha256:c07e5a0212457bdc3b3677334da99e98bbc365f4de497fc4fa39ccbf5652b6f5` was tagged `vigilvoice:local`, and only `vigilvoice_backend` was recreated with `docker compose up -d --no-build --no-deps backend`. The PostgreSQL container and named volume stayed in place; the same four counts remained unchanged and readiness returned 200 after warmup. `app/core/database.py`, `app/api/v1/actions.py` and `app/main.py` in the new image match the current checkout; mounted Silero and spoof ONNX hashes match their pinned values. No API checks wrote test records to the presentation database. Old pending verifier delivery should be treated as lost after any backend restart; create a new action/code for a later user rehearsal. This is a local candidate update, not a final frozen release.

The image was also saved locally as ignored `docs/validation/artifacts/vigilvoice-backend-sep26-checkpoint.tar` (115,613,696 bytes; SHA256 `6dbf897e74130e1ae0c9d6c37d15907913e2b8730d8b94bdca498e937c0d4a0c`); its tar inventory is readable. `docker load -i` on the **same daemon** succeeded and retained image ID `sha256:c07e5a0212457bdc3b3677334da99e98bbc365f4de497fc4fa39ccbf5652b6f5`. Paired with the 131-file source ZIP above, the unchanged PostgreSQL tar SHA256 `3e0a8f06b46ffb54b8e2b54389215960043fffbc7fc56b151f24d1baba61fb24` and runtime-model ZIP SHA256 `9314741498067ea09fbcc4fe1eea201c7d363769ad96ea3990d1f7c941363c77`, this forms a local **candidate checkpoint set**, not a fully frozen/independently hosted release. The extracted-source image was tested as described above; loading on a second daemon has not been run. The later receipts and test-harness changes are not contained in the 131-file ZIP.
