# R01 packaging checkpoint — 2026-09-24

This is a checkpoint, not the final release package. HEAD `58f7e32` is dirty and is not an immutable source identity. The new `.dockerignore` admits only `app/`, `scripts/`, `config/`, `init.sql`, `requirements.txt` and `Dockerfile` into the build context, excluding local models, research PDFs, `.env`, runtime data and evaluation outputs. `Dockerfile` pins the tested Python 3.11 base-image digest and removes the unnecessary compiler install. `requirements.txt` pins the 28 packages reported by `pip freeze` in the earlier passing Python 3.11 validation image. File SHA256: `.dockerignore` `2fcf88ce2597f7cea8499594f845967a5934c3f7d5b364a18aad6af6081ac096`, `Dockerfile` `8bb3c3926a76bb5864292aa7bfc6466b2499282da1e352baef0502cc4ad931f6`, `requirements.txt` `599379992d106c20ef3a3f7cf671493b7bf4060bc44a1493e6563100d39a1670`. Built image: `sha256:2a0331f4a8fbbd8d71fc0d7d8cb30e72cb09fcbe7710c6bdd0e222287d0c2ced`.

Checks on the isolated test database and model bind mount:

| Command/check | Result |
|---|---|
| `docker build -t vigilvoice-validation:r01 .` | Passed from the narrowed context without a compiler layer. Build fetched the pinned wheels over the Internet; this does not prove offline rebuilding. |
| Image `import app.main`, `import scripts.check_demo`; `python -m pip check` | Passed; no broken installed requirements. |
| Enumerate `/app` in image | Only `app`, `config`, `init.sql`, `requirements.txt`, `scripts`; no `.env`, private key, PDF or WAV found. |
| New `vv-r01-backend` on `vv-s02-net`, local model mount and test DB | `/readyz` 200 after startup; existing `check_demo.py` passed 19/19 live action/stream checks. |
| New `vv-r01-offline` on Docker `--internal` network with the same local DB and models, `JEV_MODE=shadow` without a key | `/readyz` 200; 19/19 live checks passed in 35.966 s. TCP connect to `1.1.1.1:443` returned network-unreachable (`101`). This verifies the local workflow without Internet egress, not an offline Docker image build. |

The existing demo stack and its PostgreSQL volume were not changed. The first R01-only test containers and internal network were removed after checks; the isolated S02 test database and stack remain. A 42-file runtime-source ZIP from the dirty worktree was extracted and built; both images contained 40 identical `/app` files with a combined SHA256 `bb98d4a2ee471c355031dbec6b1878c8dfe3221020b1be68fc964fdcef03cb8e`. Their image IDs differed, so filesystem equivalence—not byte-identical image identity—is claimed. The extracted build passed `/readyz` and 5/5 hard-SIGKILL restart checks in 2.102 s against the preserved isolated test database. The source ZIP included untracked `jev.py`, the manifest validator and the Jev evaluator.

An [offline package checkpoint](offline-package-2026-09-24.md) now binds a 44-file source ZIP, backend and PostgreSQL image archives, and a model/public-demo archive by SHA256. Both images loaded into the same daemon with expected IDs; the extracted source Compose file selected their tags, and extracted model/demo hashes matched `models/manifest.json`. The superseded test-tag backend tar was removed after the `vigilvoice:local` archive was verified. Large image/model archives are local ignored files, not Git artifacts.

R01 remains IN_PROGRESS: this is not a final source freeze or a pristine-host Compose run, and no upgrade rehearsal against the existing demo database was performed. Exact package versions and base digest are pinned, but wheel hashes are not; rebuilding still needs a package index unless wheels are preloaded. Runtime requires the verified local model files. Do not call this an immutable release or minimum-hardware proof.

## September 24 v03 checkpoint after S03/V02

The first source/backend archives above were superseded after the browser and same-session WAV admission fixes. The [updated offline manifest](offline-package-2026-09-24.md) identifies new `v03` source/backend archives and hashes while reusing the unchanged PostgreSQL and model/public-demo archives. The `v03` backend image ID is `sha256:136591533727ec788a0e6365adf395ceefe562c3e856637b8ec36efa0f51c2d4`. The source ZIP was staged from that image's exact 40 `/app` files plus Dockerfile, `.dockerignore`, Compose and README; extraction matched all 44 file hashes and contained no secret, audio, model or PDF. Compose config validated with placeholder credentials, without creating a `.env`. The backend tar loaded into the same Docker daemon at the expected image ID.

On a temporary Docker `--internal` network, the exact `v03` image reached `/readyz` 200, rejected external TCP with `ENETUNREACH` 101, and passed the updated 20/20 API/action suite in 40.385 seconds with the isolated local database and read-only models. The temporary container and internal network were removed; original demo containers/volume and the test database were retained. The [S03/V01 receipt](s03-2026-09-24.md) and [V03 runtime checkpoint](v03-2026-09-24.md) cover the final-image browser, ten-minute stream and hard restart. R01 still lacks a *pristine-host* Compose launch, a final-source freeze and a complete bundled documentation tree. The current archives are a local checkpoint, not release artifacts.
