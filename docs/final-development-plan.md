# VigilVoice final development plan and progress

Last updated: **2026-09-26 (Asia/Calcutta)**. Planning baseline: `396f36c9d4f3e48fba4ebaa3aad7a670daf28af4`, with pre-existing local configuration/runtime changes.

Presentation target: **2026-10-03**, derived from the user's instruction on September 13: "in 20 days". Day 0 is September 13; Days 1-20 are September 14-October 3. Confirm the exact presentation time during rehearsal scheduling.

User requirement: a presentation-ready system with a fully working backend and a model that detects synthetic/cloned speech. Pretrained models, fine-tuning, or training are all acceptable; team-owned training is not a requirement. Save context and always record progress. This session authorizes planning and persistence; implementation, publication and spending must follow the user's subsequent instructions and existing repository authority rules.

## Resume here

- **Current stage:** B01-B05, D01, S01-S03, V01 and V04 have passing receipts. M01's rich evaluator now separates validation/final-test inference and uses the frozen <=10% genuine HIGH false-block bound for validation candidate selection; no rich corpus has been scored. V02's isolated PostgreSQL pause now returns a bounded 503 on the exact-source image with zero challenge/completion events for the exercised request, and 22 API groups pass. A pre-COMMIT pause also proved a sent COMMIT can take effect despite a client timeout, so V02 remains open. The current-build physical live-mic retest connected and produced LOW/ELEVATED/HIGH results, with tested actions blocked by HIGH or later poor-quality evidence. User-reported microphone-to-WAV actions completed on the current demo at ELEVATED and LOW; persisted action, verification, approval and audit state were checked. The user confirmed a second-person verifier for the connected ELEVATED run and reported all four requested elements for a later offline LOW run. Its LOW completion is persisted, but the demo DB has no corresponding HIGH/BLOCKED event during that run; reconcile before crediting full R03 acceptance. M04 robustness remains unvalidated. R02's deck and handbook PDFs are refreshed and visually reviewed, but narrated video and final-source checks remain. D02 remains blocked on rights/consent and independent four-language sources. Jev shadow has no live key/evaluation.
- **Next task:** Obtain permitted, source-disjoint D02 audio and run representative Stage 0 before model or threshold selection; if unavailable on September 26, defer detector replacement explicitly. For V02, the injected post-COMMIT route checks pass, but a real transport pause at exact COMMIT timing and frozen-release recheck remain; an ambiguous 503 requires checking persisted status before retrying. Keep live-action evidence fail-closed; do not extend its TTL or change HIGH policy based on one session. Reconcile the user-reported synthetic HIGH/BLOCKED offline checkpoint with the missing current-demo DB event and capture full-run timing; complete remaining R03 runs and R02 assets. Keep M04 policy changes evidence-gated and Jev non-advisory. R01's `v03` offline archives need rebuilding after source freeze.
- **Known release blocker:** representative four-language data, a defensible model/threshold freeze, physical-device rehearsals and final-source release verification remain missing. Dependencies and Docker context are now pinned/narrowed; Python 3.11 Docker remains the verified release runtime.
- **Critical path:** B01 -> B02/B03 -> S01/S02 -> M02/M03 -> V01/V02 -> R01/R02 -> R03/R04. Dataset work D01/D02 starts alongside backend work because model selection depends on it.
- **Next checkpoint:** September 26 model/policy freeze decision. If permitted disjoint data is still unavailable, explicitly defer replacement and disclose the unmet accuracy gate rather than tuning the historical clips.
- **Final acceptance:** NOT VERIFIED. B02-B05, S01-S03 and V01 prove the exercised backend, readiness, protected-action recovery, bounded sequenced streaming and fake-device browser-to-action paths; representative evaluation, physical-device evidence and final release checks remain open.

## What "fully working for presentation" means

The final build must capture real microphone audio continuously, run actual local model inference, show timely risk and reasons, and enforce the complete protected-action workflow in the backend. It must also support recorded WAV input, persist actions/audit, and recover safely from failure or restart. A direct API call must never bypass the controls displayed in the UI.

The demonstration uses a **simulated INR transfer and local verifier delivery**, as explicitly allowed by Context.pdf and the existing handbook. The model, API, database, verification checks and audit must execute for real. The local verifier illustrates a separate role; it is not a real independent authentication channel. Real banking/SMS integration, production hosting, telecom adapters and multi-tenant accounts are outside this 20-day presentation scope.

Retain one detector in the inference path, Silero VAD, FastAPI, PostgreSQL, ONNX Runtime and native browser code. Reuse existing services, routes and test scripts. Do not add an ensemble, Redis, Kubernetes, WebRTC, speaker verification, watermarking or a new frontend framework for this milestone. Optimize or replace the detector only when measurements justify it.

Preserve the existing strict policy: LOW/ELEVATED can request independent verification; HIGH, unusable/missing/stale evidence and service failure prevent completion. Every simulated transfer requires valid, fresh, action-bound, single-use approval. A lower risk score never authenticates a person.

## PDF requirements and current implementation

All page references below are physical PDF pages, starting at 1. The four PDFs were read during planning; original proposal claims are requirements, not proof of implementation.

| Source | Relevant requirements | Development consequence |
|---|---|---|
| [Context.pdf](../Context.pdf), pp. 1-7, 16-28, 37-43 | Prevention first; Web Audio/AudioWorklet -> authenticated PCM/WebSocket; quality, one detector, action-bound OTP and audit; four language groups; duration/channel/replay/unseen-generator evaluation; six demo cases; offline fallback | Governs the final demo pipeline, test matrix and proof package. Its original 36-hour schedule is superseded by this user's 20-day deadline. |
| [SIH idea deck](<../SIH2026-IDEA-Presentation-Format final 2.pdf>), pp. 1-6 | SIH26104, team Power Rangers; continuous analysis; 2s evidence with 0.5-4s study; calibration; 6-digit/60s/single-use OTP; detection at fixed FPR, first-alert latency and false alerts per genuine call | Implement and measure continuous operation. Revise the final slides to name the actual selected model and measured results. Existing claims of Indian-language/channel validation are not yet supported. |
| [Teacher presentation](teacher-presentation.pdf), pp. 3-7 | September 25 refreshed account of streaming, guarded action flow, historical 40-clip regression and explicit pending work | Re-render against the final frozen source; do not promote its historical English results to a target-language claim. |
| [Build and launch handbook](VigilVoice-Build-and-Launch-Guide.pdf), pp. 2-10 | September 25 refreshed architecture, Docker readiness, operator/verifier screens, expiry rules, checks, recovery and local fallback | Recheck commands and evidence after final-source freeze and keep the silent video labeled historical. |

Decisions resolving differences between PDFs:

- The user permits any effective model, so a newly trained frozen WavLM head is optional. The current pretrained Wav2Vec2 classifier is the baseline candidate.
- Continuous browser streaming remains required; record-then-upload is the fallback, not completion of the streaming requirement.
- Keep existing `/api/v1` contracts unless a measured requirement needs an additive change. For example, the current stream route is `/api/v1/stream/ws/{session_id}`; do not rename it just to match the illustrative PDF path.
- Existing PostgreSQL tables and version digests may implement the PDF concepts without creating a separate table or worker process for every illustrated box.
- Threshold selection is required. Probability calibration is conditional on having suitable data and an explicit probability claim; otherwise expose model scores and thresholded risk, as Context.pdf p. 10 allows.

## Verified baseline and gaps

| Area | Evidence as of September 13 | Final work remaining |
|---|---|---|
| Local Python checks | B03: **20 passed, 1 skipped**, 22 validation subtests; Python 3.11 focused readiness: **8 passed** | Final-source regression and later-stage failure/stress evidence remain required. |
| UI logic checks | S03/V01: VM permission/pagehide and headless Chrome fake-device stream, WAV fallback, action-bound genuine completion and verified-action live HIGH denial passed | Physical microphone and OS permission interaction remain unverified. |
| Protected actions | B04: ownership, payload/action binding, expiry, OTP lockout, atomic replay/race behavior, audit rollback and hard-restart denial/recovery passed | Revalidate on final source and during later active-stream failure/capacity work. |
| Audio | Local Silero + INT8 Wav2Vec2; native 16 kHz AudioWorklet sends authenticated 200 ms PCM16 frames; headless Chrome reached LOW at 1.5s while capture continued | Backlog/freshness and long-run physical-device capacity remain unverified. |
| Models and evaluation | Historical 40 English clips; default test missed 1/9 scorable spoof and falsely blocked 1/9 scorable genuine; candidate 0.58 falsely blocked 3/9 genuine | Representative language/channel evaluation and defensible threshold choice. Candidate threshold is not deployed. |
| Timing | Historical offline median 3.06s, p95 7.79s per file; model load 7.07s | Capture-to-alert, API overhead, warm startup and limited concurrency on the presentation laptop. |
| Packaging and readiness | B01 source repair plus B02 clean build; B03 liveness/readiness verifies DB, full runtime schema, executable models and verifier | Dependency pinning, `.dockerignore`, offline package and final immutable source/model receipts remain. |
| Presentation assets | Existing slides, handbook, screenshot and 30-second silent video | Refresh for streaming and final metrics; existing video omits the synthetic HIGH/BLOCKED checkpoint. |

Historical evidence: [teacher build receipt](teacher-build-receipt.json), [API checks](demo-checks.json), [browser checks](browser-checks.json), [evaluation report](evaluation-report.md), [model setup](model-setup.md). Existing Ruflo task rows are stale; a prior receipt says its CLI could not mark them complete. The task ledger below is authoritative for this milestone.

## September 24 inspection and change order

The inspected pre-change path was browser AudioWorklet (16 kHz mono PCM16, 200 ms frames) or strict PCM WAV upload -> Silero VAD/quality and a two-second rolling window -> local INT8 wav2vec2/XLS-R ONNX classifier -> EMA policy -> session evidence -> deterministic action checks -> action-bound 60-second, three-attempt OTP -> single-use approval -> same-transaction audit. The WebSocket authenticates in its first text frame. One stream and one CPU inference slot are admitted. Jev was absent from that baseline and cannot replace acoustic inference or authorization.

Pre-change checks on the September 24 checkout: `python -m pytest -q -p no:cacheprovider` passed 23, skipped 1 (26 subtests); UI and microphone smoke passed; local-model smoke passed 5; the running Docker `/readyz` returned 200. Final-source checks and the limited sustained-stream measurement are in the [S02 receipt](validation/s02-2026-09-24.md). None establishes production minimum CPU/memory requirements.

The immediate fault found was that `record_evidence` advanced `latest_evaluation_id` on each result, so a HIGH result could be displaced by later LOW while an action was PENDING or VERIFIED. The implemented shared fix blocks and audits unfinished actions in the same transaction when HIGH arrives; an old action cannot be revived by later genuine speech. Stream replacement/disconnect, bounded frames and server-owned receipt age followed. The revised wire format is explicitly negotiated as `pcm16-seq-v1`, and legacy unsequenced binary is rejected. Ownership, OTP, restart and browser checks were rerun; see the [S02 receipt](validation/s02-2026-09-24.md).

Jev is now a metadata-only optional branch after the deterministic safety gate. `disabled` never calls it; `shadow` (default) records a typed recommendation but leaves policy unchanged; `advisory` may only escalate an eligible soft decision and must not be enabled until live shadow evidence meets the predefined zero-unsafe-downgrade target. No request holds an action/session database lock over the network; locks and hard checks are repeated before a commit. Invalid responses, missing credentials, timeout and offline operation fall back to deterministic policy; no Jev choice can complete an action. The Jev API contract uses a pinned model and a `choice` question with four non-approval options, not the illustrative contract in the supplied note.

Dataset work begins with license/access/lineage metadata, then a source-linked manifest and leakage validator; no bulk audio download or gated-access acceptance is implied. Stage 0 measures the deployed detector on approved validation data. A frozen pretrained SSL head is conditional on Stage 0 failure and sufficient disjoint labeled data/GPU resources. The 40 existing English clips are regression material, not four-language validation. Freeze thresholds and preprocessing on validation before one untouched test. Jev shadow metrics and detector accuracy claims remain unverified until real inputs exist.

## Reference repositories reviewed September 24

These are design references, not validated VigilVoice components. No external code, weights, datasets or new dependencies were imported.

| Reference at reviewed commit | Relevant insight | Plan decision |
|---|---|---|
| [MarkHershey/AudioDeepFakeDetection](https://github.com/MarkHershey/AudioDeepFakeDetection/tree/602bc495439826cefb7cc15f9172e50b951beaa3) | Compares waveform, MFCC and LFCC features and in-/out-of-generator settings using LJSpeech and WaveFake. Its README explicitly says the test set is used for training validation/model selection. | Keep the existing held-out-generator and source-linked split requirements; do **not** import its model, random/file-order split or reported scores. A lightweight LFCC control is optional only if the approved D02 corpus exists and a measured runtime/accuracy question remains after the baseline/SSL comparison. No extra deployed ensemble. |
| [Guri10/Deepfake-Audio-Detection-with-XAI](https://github.com/Guri10/Deepfake-Audio-Detection-with-XAI/tree/0ada89e873456582298189390b348b3c2aa9b4f0) | Spectrogram-image CNN notebooks apply LIME, SHAP and gradient maps; the repository gives no demonstrated compatibility with our 2-second streaming path or cross-language/held-out-generator performance, and no repository license was identified. | Do not copy code/weights or add an image-CNN/XAI dependency for the presentation. If labeled partial-spoof spans become available **after** the detector and policy are frozen, consider an offline, evaluation-only temporal explanation-fidelity probe; do not treat a heatmap as an acoustic cause or let it affect action authorization. |
| [yibie/awesome-jev](https://github.com/yibie/awesome-jev/tree/8be6425a5d63e4ef9da020e328e1eae55064b223) | Curated Jev examples show typed choices, deterministic thresholds, mock/replay harnesses and calibration checks, but catalog entries are not evidence that a linked integration is safe here. | Retain metadata-only shadow and the deterministic hard gate. For J02, replay sanitized state/answer records against the same policy offline, count malformed/off-menu/probability-invalid replies as failures, and assess per-choice calibration only with labeled live cases. Never add an `ALLOW_TRANSFER` choice or promote advisory from fixture agreement. |

Reference-informed additions are conditional acceptance details for M01/M02 and J02, not new critical-path features. The priority remains representative validation and fail-closed/offline operation; no training, XAI display or live Jev call is authorized by this review.

## Twenty-day schedule

These are execution windows, not claims of work completed. A missed gate is recorded as blocked or incomplete; it is not silently waived to meet the date.

| Window | Dates | Deliverable and checkpoint |
|---|---|---|
| Days 1-2 | Sep 14-15 | Reproducible source/backend baseline; select target laptop/regional language; freeze dataset protocol and numerical targets before tuning. |
| Days 3-5 | Sep 16-18 | Reliable readiness, validated configuration, protected-action failure handling and restart recovery. Acquire/label data in parallel. |
| Days 6-8 | Sep 19-21 | Continuous microphone-to-backend PCM streaming, fresh risk display, bounded backlog and failure handling. Finish evaluation corpus. |
| Days 9-11 | Sep 22-24 | Benchmark baseline on development/validation data; compare one replacement or train a lightweight head only if needed. Measure duration/channel/quality failures. |
| Days 12-13 | Sep 25-26 | Select model and policy on validation, freeze artifacts and run untouched final test. Stop model experimentation by end of Day 13. |
| Days 14-16 | Sep 27-29 | Full browser/API prevention tests, overload/restart/failure injection, physical microphone and latency/capacity checks; fix defects. |
| Day 17 | Sep 30 | Freeze final source/image/model/profile; generate final evidence, deck, guide and backup video from that exact build. |
| Days 18-20 | Oct 1-3 | Three full rehearsals, offline recovery practice and presentation. Reserve for defects and delivery, not new features. |

## Task ledger - update this table after material progress

Statuses: `TODO`, `IN_PROGRESS`, `BLOCKED`, `DONE`, `DEFERRED`. `DONE` requires its completion check plus linked evidence. Owner labels are roles, not claims that a worker is currently running. Initially only PLAN is done.

| ID | Window / owner | Status | Work and completion check | Evidence / next action |
|---|---|---|---|---|
| PLAN | Day 0 / integration | DONE | Read PDFs/source, save agreed 20-day scope, task ledger and session instructions | This document; AGENTS.md and CLAUDE.md entry points; Day 0 log below |
| B01 | Days 1-2 / integration | DONE | Correct the broad model ignore rule without exposing weights; ensure `app/models/schemas.py` and all required imports are included in the reviewable source; verify an isolated clean source export builds and imports the app | Root `/models/` remains ignored; `app/models/schemas.py` is addable; clean export tests 12 passed/1 skipped; image `sha256:1a7e7e98a2ff3f8f18dca0f26023c8cdd2292b2d10e9b63df400b8ab48236ef7` imported `app.main` on Python 3.11 |
| B02 | Days 1-2 / backend | DONE | Reproduce local Docker startup, additive migration twice, model checksum checks and existing unit/API checks; save environment and exact-source baseline evidence | [B02 receipt](validation/b02-baseline-2026-09-13.md): rebuilt stack, four idempotent migration runs, pinned checksums, real-model smoke, unit/UI and two 13-group API runs passed; existing DB volume preserved |
| D01 | Days 1-2 / evaluation | DONE | Choose regional language from accessible permitted data, inventory sources/consent and freeze split, metrics, latency and false-block targets before tuning | [Frozen protocol](evaluation-protocol.md): Marathi selected; sources/access/consent inventoried; source grouping, untouched test, manifest fields and numerical targets frozen before tuning |
| B03 | Days 3-4 / backend | DONE | Add readiness for DB/schema, both models and demo verifier; bounded model warmup; validate finite/ordered thresholds, durations, positive TTLs and consistent OTP limits | [B03 receipt](validation/b03-readiness-2026-09-13.md): full schema/executable-model/verifier readiness, fail-closed dependency cases and startup validation passed; final review found no blocker |
| B04 | Days 3-5 / backend | DONE | Verify action state machine, ownership, payload binding, approval replay/races, audit rollback and post-restart denial/recovery | [B04 receipt](validation/b04-actions-2026-09-13.md): 14 API groups, injected audit rollback, 5 hard-restart groups, focused unit/UI checks passed; zero unauthorized completions |
| B05 | Days 4-5 / backend | DONE | Define single-worker capacity and verifier restart behavior; bound upload/auth/inference waits and concurrent sessions; correct expired-challenge and unavailable UI recovery | [B05 receipt](validation/b05-capacity-verifier-2026-09-13.md): one active audio session, sessions 2/3 rejected promptly, bounded DB/upload/inference waits, explicit lost-delivery expiry/new-action recovery, 15 live and 5 hard-restart groups passed |
| D02 | Days 2-8 / evaluation | BLOCKED | Assemble four-language source-disjoint corpus with real/synthetic labels, quality/channel annotations, license/consent and hashes; keep demo/training/test separate | [Acquisition matrix](dataset-acquisition.md) and [rich manifest contract](manifest-schema.md) prepared; validator and leakage tests pass. Needs accepted access, source/target lineage, consented Hinglish recordings and a permitted held-out generator before corpus assembly |
| S01 | Days 6-7 / browser + backend | DONE | Add native AudioWorklet capture/resampling and authenticated WebSocket client; await ready then stream mono 16kHz PCM16 at a measured cadence; retain recording/WAV fallback | [S01 receipt](validation/s01-browser-streaming-2026-09-13.md): Chrome produced eight live updates while capture continued, LOW at 1.5s, WAV fallback passed; live API and repaired client passed |
| S02 | Days 7-8 / backend | DONE | Bound capture/transport/inference backlog; use server-owned receipt/freshness information; stale/duplicate/out-of-order/gapped input cannot refresh old evidence as new speech | [S02 receipt](validation/s02-2026-09-24.md): final image 19/19 live API, 5/5 hard restart, 150-frame short stream; earlier image 2,999-frame/600-second stream. Physical microphone remains S03/V03/R03. Legacy unsequenced wire format is intentionally rejected. |
| S03 | Days 7-8 / browser | DONE | Display live risk, evidence age, reasons and action status; handle stop, permission denial, disconnect and reconnect; release devices on navigation | [S03/V01 receipt](validation/s03-2026-09-24.md): server-owned age, bounded refresh, VM permission/pagehide checks, isolated Chrome stop/reconnect and action status checks passed. Physical device remains V03/R03. |
| J01 | Sep 24 / backend | IN_PROGRESS | Optional Jev client and metadata-only shadow audit; hard gates, timeout, typed choice, circuit, offline fallback; action recheck after network | Client, mocks, no-key isolated action checks and [architecture note](jev-integration.md) pass; no key for a real shadow response, so external contract/latency not verified |
| J02 | Before advisory / evaluation | BLOCKED | Labeled live shadow scenarios and agreement, unsafe downgrade, escalation, review, latency, failure and calibration report; replay sanitized live answer records through the frozen deterministic policy | 20 policy fixtures pass with zero unsafe downgrades but are not service observations; needs Jev access and permitted labeled cases. Count malformed/off-menu/probability-invalid replies as failures; assess calibration only with real labels; target zero unsafe downgrades. [Reference review](#reference-repositories-reviewed-september-24) |
| J03 | After J02 / integration | DEFERRED | Permit advisory escalation only after the predefined shadow target and security review; keep action executor deterministic | Code path is feature-gated and mocked; advisory is not approved for presentation until J02 passes |
| M01 | Days 9-10 / evaluation | IN_PROGRESS | Extend existing evaluator for language, duration, channel, replay, held-out generator and partial-manipulation slices; log raw/scorable/rejected counts | EER/AUC/precision/F1/coverage/no-alert and metadata slices added; [phase-separation receipt](validation/m01-phase-separation-2026-09-24.md) proves rich validation does not score test. [Selection receipt](validation/m01-selection-2026-09-25.md) adds the pre-frozen <=10% false-block candidate objective and cross-role speaker leakage check; no approved rich corpus was scored. Duration/replay/partial-spoof samples, intervals and first-alert distribution remain unavailable. Optional post-freeze explanation-fidelity diagnostic needs labeled partial-spoof spans and cannot change the action gate. [Reference review](#reference-repositories-reviewed-september-24) |
| M02 | Days 9-11 / model | IN_PROGRESS | Measure existing detector; if validation misses agreed targets, compare one suitable local candidate or train a lightweight frozen-encoder head using separate training data | [Stage 0 English regression](validation/stage0-2026-09-24.md) misses frozen targets; no representative four-language corpus or clean held-out family, so replacement/training deferred pending D02 |
| M03 | Days 12-13 / model + evaluation | TODO | Select thresholds on validation; freeze model, preprocessing, cadence, quality gates, smoothing and policy; evaluate untouched test once | Hashes, per-slice metrics and confidence intervals; probability calibration only if supported and explicitly tested |
| M04 | Days 11-13 / audio | IN_PROGRESS | Validate noisy/all-speech SNR behavior, clipping, narrowband/phone audio and short evidence; fix demonstrated quality failures and retest before freeze | [Public-clip probe](validation/m04-public-probe-2026-09-24.md): synthetic LOW after added noise/band-limit in both host and isolated Docker runtime; two demo clips cannot justify a threshold or quality-policy change. Real channel/representative data remain. |
| V01 | Days 14-15 / tester | DONE | Exercise complete genuine approval and synthetic blocking in actual browser/API/PostgreSQL flows, including active-stream risk replacement during verification | [S03/V01 receipt](validation/s03-2026-09-24.md): genuine action completed after OTP; verified action blocked by same-stream synthetic HIGH and direct completion returned 409 even after later LOW; synthetic upload/new action blocked; action audit visible. Fake microphone, not physical. |
| V02 | Days 14-16 / tester | IN_PROGRESS | Prove expiry/3-attempt lockout, reused/wrong-action tokens, foreign sessions, concurrent completion, malformed/oversized audio, stream loss, model/DB/audit failure and restart safety | [S03/V01 receipt](validation/s03-2026-09-24.md): API 20/20. [V03 checkpoint](validation/v03-2026-09-24.md): 5/5 hard-restart checks. [Commit-failure receipt](validation/v02-commit-failure-2026-09-24.md): injected failure rolled back. [Paused-DB receipt](validation/v02-database-timeout-2026-09-25.md): exact-source isolated image returns 503 in 2.15 s with no issued challenge/completion for the exercised request, API checks pass 22/22, and three injected post-COMMIT route cases preserve retry/approval/audit invariants. A real transport pause at COMMIT timing and release-image recheck remain. |
| V03 | Days 14-16 / performance | IN_PROGRESS | Measure cold/warm start, WAV latency, live first-alert and evidence age, 1/2/3-session stress and long-run stability on presentation hardware | [V03 checkpoint](validation/v03-2026-09-24.md): final image 12 warmed 10-second WAVs median 5.19 s/p95 5.26 s; 2,999 frames/600.2 s with 11.83 ms median/400.01 ms p95 response and stable sampled 784 MiB backend RSS; warm restart-to-ready 13.31 s. One demo synthetic clip first HIGH at 2.4 audio seconds. User-reported physical mic and WAV transfer completion on the current build is recorded below; current-build capture timing, full cold start, first-alert distribution and sustained multiuser work remain. |
| V04 | Days 15-16 / reviewer | DONE | Check session/verifier separation, bounded unauthenticated endpoints, SQL/input/output safety, no raw-audio or plaintext-token logs, retained-data behavior and dependency issues | [V04 receipt](validation/v04-2026-09-24.md): 60/min session and eight pre-auth socket single-process caps, 21/21 live API and 15/15 fake-device browser checks, 31 Python passes/26 subtests, point-in-time dependency audit with no known advisories. Indefinite DB retention and production DoS/TLS limits disclosed, not solved. |
| R01 | Day 17 / integration | IN_PROGRESS | Package exact tested dependencies/images/models, safe Docker context and offline startup; verify new clone/source export plus preserved-db restart | [R01 checkpoint](validation/r01-packaging-2026-09-24.md) and [offline package](validation/offline-package-2026-09-24.md): `v03` archives stale; reproducible source/docs packager closes broken README links, extracted source builds/starts, and current no-egress Jev-disabled runtime passed 22/22 API groups with external TCP unreachable. Needs final image/model archives, pristine-host Compose launch, preserved-DB rehearsal and source/model freeze before DONE. |
| R02 | Day 17 / presentation | IN_PROGRESS | Refresh SIH deck/handbook, six-case script, Q&A, measured metrics and narrated backup video including HIGH/BLOCKED | [Rehearsal guide](teacher-demo-guide.md), [runbook](prototype-runbook.md) and [evaluation guide](evaluation-guide.md) describe current streaming and limits. [Asset receipt](validation/r02-assets-2026-09-25.md): existing seven-slide deck and ten-page handbook PDFs refreshed, page previews visually checked; exact-source isolated fake-device browser/action run passed 15 checks, including HIGH/BLOCKED with screenshot. The current-build recorded-mic/WAV action completed. Narrated current-build video, separate-verifier/offline rehearsal and final-source re-render remain. |
| R03 | Days 18-19 / presenter + tester | IN_PROGRESS | Run at least three timed full rehearsals with real laptop microphone, separate verifier role, projector and internet disconnected | September 25 connected ELEVATED run: user-confirmed second-person `/verify`, persisted completion. September 26 offline LOW run: user confirmed live mic, separate verifier, projector and synthetic HIGH/BLOCKED; LOW action completed in 17.93 s with verified challenge, consumed approval and completion audit. Demo DB has no HIGH evaluation or BLOCKED action in that run's window; full-run elapsed time also unrecorded. Reconcile before accepting as one of three timed full rehearsals. |
| R04 | Day 20 / integration + presenter | TODO | Check final readiness, checksum-selected artifacts, local clips, verifier access and backup media; close or disclose every release blocker | Final acceptance checklist and handoff; no automatic external deployment/publication |

## Backend implementation notes

Start with `app/api/v1/{sessions,stream,actions}.py`, `app/services/{action_gate,audio_evidence,audio_pipeline,demo_verifier,audit}.py`, `app/core/{config,database}.py` and the existing migration. Preserve row-locking, generation fencing and transaction-bound audit rather than rewriting the backend.

Specific inspected gaps to address:

1. `app/models/schemas.py` is required but ignored/untracked. Fix reproducibility before adding features. The application stack is Python/FastAPI; generated Ruflo TypeScript/npm quick-start instructions are not the application build commands.
2. `/healthz` returns static OK. `/api/v1/system` checks model state but not DB readiness, and the UI currently considers only the spoof detector. Readiness must reflect the complete serving path, without blocking each probe behind expensive inference.
3. The stream accepts a first JSON token frame, then binary PCM, and returns `ready`. Its current check opens and closes a socket without sending speech; `scripts/test_client.py` creates an unregistered UUID and sends PCM before auth. Repair these contracts together.
4. One CPU inference thread currently serves uploads and streams. Keep session VAD state isolated and serialize each session's calls. Separate capture cadence from expensive inference cadence if profiling requires it, and preserve every required quality/freshness check. Do not increase evidence TTL to hide slow processing.
5. Processing time is not capture time. Stream backlog, pauses and replayed buffered frames need bounded age/gap rules so old audio cannot be written as fresh safe evidence. Client-provided timing alone is not authoritative. Verify that HIGH observed during a pending action cannot disappear behind later genuine speech before completion; retain the relevant action-level block/evidence, with explicit new-action recovery.
6. Verification challenges/audit persist; the local delivery inbox does not. Use one backend worker for this scope, demonstrate safe restart/new-action recovery and do not introduce plaintext OTP persistence merely to survive restart. Completed actions and audit must survive restart.
7. Validate configuration at startup and bound network waits/input sizes. Preserve constant-time secret comparisons, hashed tokens, maximum three OTP attempts and existing generation/ownership checks.
8. Replace misleading reason labels such as `VERIFIED_LOW_RISK` and `HIGH_SPOOF_PROBABILITY` with evidence-based language, updating all callers/tests. The default UI must never equate LOW with verified identity.

## Detection, data and measurement protocol

Use the existing evaluator and manifest validation in `scripts/evaluate_audio.py` and `scripts/prepare_evaluation.py`. Add only the metadata and reporting needed for this matrix; a new experiment platform or evaluation database is unnecessary.

- **Languages:** Hindi, Indian English, Hinglish and one regional language chosen in D01. Initial acquisition target: at least 40 genuine and 40 synthetic independent source recordings per language (320 total), split 20+20 per class into validation and test. Training, if needed, uses additional separate data. Count speakers and generator families, not just files. This is a presentation-scale target, not a population accuracy guarantee.
- **Provenance:** permitted public sources or consented recordings; record source, license/consent, language, speaker/source group, generator, duration, channel and hashes. Dataset names in the PDFs are candidates to verify for access/license; they have not all been downloaded or approved for this project.
- **Independence:** keep speakers, source recordings, related transcripts/augmentations and derivative clips together. Reserve generator families from any team-controlled fitting/selection; distinguish this from unknown overlap with a pretrained model's original training data. Never use final test clips to select the demo, threshold or candidate.
- **Conditions:** clean; real phone recording or clearly labeled simulated narrowband/codec transformation; noise; low-volume/accented genuine speech; speaker-to-microphone replay; held-out generator; genuine -> inserted synthetic -> genuine. Include at least 10 genuine and 10 synthetic source recordings in each supplemental stress condition where available; report unmet coverage instead of inflating counts with duplicates. Augmentations remain linked to their original sources.
- **Duration:** evaluate 0.5, 1, 2 and 4 seconds without repeating audio to manufacture speech. With the current 1.5s minimum speech gate, short clips should report insufficient evidence. If diagnostic detector-only results are produced, label them separately; do not weaken the deployed minimum merely to fill a duration table.
- **Detection target, proposed for D01 freeze:** at least 90% HIGH detection among scorable synthetic test clips at no more than 10% HIGH false blocks among scorable genuine clips. Choose the threshold at the predeclared FPR on validation, then report actual test FPR/recall with numerators and 95% confidence intervals. Report each language and stress condition separately. A weak slice stays a failed target; aggregate performance cannot erase it.
- **Coverage target:** at least 85% scorable coverage for both genuine and synthetic supported-quality baseline clips, and zero unexplained model/service failures at supported load. Count quality rejection, insufficient evidence and unavailability separately for each class. Blocking everything is not detection success.
- **Latency targets, proposed for D01 freeze:** p95 first HIGH alert within 4 seconds of synthetic onset among detected, scorable 2-second-evidence attack trials. Report every no-alert/miss separately, and target at least 85% alerted within 4 seconds using ALL scorable attack trials as the denominator. Also target p95 warmed end-to-end analysis <=10 seconds for a 10-second WAV and no growing stream backlog over a 10-minute call. Report cold-start time separately and warm the demo before presentation. If hardware cannot meet these targets, optimize measured work or select a faster detector before model freeze.
- **Capacity:** the release must support one live operator stream while verifier/action/audit endpoints remain responsive. Test 2 and 3 streams to establish limits; explicitly reject excess load if it cannot be supported. Do not advertise unmeasured multi-user scale.
- **Metrics:** HIGH recall at fixed FPR, spoof still eligible for verification, false HIGH blocks, false alerts per genuine call, scorable coverage, quality/unavailable counts, inference time, capture-to-first-alert median/p95, and sensitive-action success/denial. Keep detector misses distinct from unauthorized action completion.
- **Selection:** tune on development/validation, freeze by Day 13, then run final test. If final test fails, record the failure; further tuning requires a new untouched holdout and new evidence. Do not repeatedly tune against the same test set or silently lower acceptance targets.

If the baseline fails validation by Day 10, spend Days 10-11 on one evidence-backed candidate change or lightweight-head training. No full encoder training or multi-model expansion is planned. Any paid compute, external voice upload or restricted data acquisition needs the authority actually required for that action; availability is not assumed.

## Final acceptance and evidence

The following are release checks, not additional task statuses. Keep task status solely in the ledger.

1. Reproducible source export builds; all runtime imports are included; migration is additive/idempotent; required models are pinned and locally available; fresh readiness passes before the audience demo.
2. Genuine/eligible speech -> exact action -> separate verifier -> valid code -> single completion -> persistent audit works through the actual browser and API. HIGH or unsafe evidence cannot complete even with an earlier valid approval.
3. Every negative authorization/failure case in V02 denies completion; database/audit failure rolls back the action; backend restart preserves completion history and never revives expired approval.
4. Live speech produces continuously refreshed evidence with bounded lag. Disconnection, stale input, permission loss and source replacement invalidate it. Actual capture-to-alert and supported capacity are measured.
5. A frozen detector and policy meet the predeclared targets on the supported presentation conditions, with per-language/condition results, denominators, confidence intervals and limitations. Unmet targets remain explicit blockers to claiming those capabilities complete.
6. Three full physical-device rehearsals pass. Demonstrate six PDF cases: genuine approved flow, synthetic block, degraded synthetic audio, difficult genuine audio, held-out generator and system failure. Every displayed outcome is actual measured behavior; a missed detection is disclosed, not replaced with a fabricated score.
7. The final deck, handbook, video and metric panel reference the same source/model/profile. Raw private recordings, credentials and plaintext OTPs are excluded from logs/source/evidence.

Reuse these commands when executing B02 and final validation; check the relevant environment first:

```powershell
python -m pytest -q
node tests/ui_smoke.cjs
node tests/microphone_smoke.cjs
# Explicitly enable local-model smoke; use an environment with ONNX dependencies and verified models.
$env:RUN_MODEL_SMOKE = '1'
python -m unittest discover -s tests -p test_audio.py -v
Remove-Item Env:RUN_MODEL_SMOKE
```

The API/browser scripts in [the demo guide](teacher-demo-guide.md) create simulated sessions/actions. Run them against an isolated test database/stack with validated ports for failure/restart work. Never reset the existing demo database or delete its volume. Update `scripts/check_demo.py` and `scripts/check_browser.py` to cover streaming/full-action checks rather than creating parallel harnesses.

During implementation save evidence under `docs/validation/` and final evaluation under `docs/evaluation-output/` (currently ignored; use a deliberate sharing/export decision for reports). Each final receipt must name the command, timestamp, environment/hardware, result, exact source snapshot, dependency/image versions, model/profile and dataset hashes. A dirty tree requires a snapshot including necessary tracked AND untracked files; HEAD alone is insufficient. Record artifact locations and hashes, not secrets or private audio. No placeholder pass results.

## Future-session and progress rules

1. At every new session, read this file, the latest session log and `git status --short` before planning or editing. Resume the first unfinished dependency-ready task unless the user steers elsewhere. Do not recreate a separate plan or assume stale Ruflo rows describe current progress.
2. On starting a task, mark its row `IN_PROGRESS`, name the actual worker/session and exact scope, and update "Resume here". One writer per worktree; any other writing agent needs an isolated worktree and path ownership. Read-only reviews may share the checkout.
3. After each material change/test, update that row with progress and evidence. Partial implementation remains `IN_PROGRESS`; document remaining checks. Record failures as faithfully as passes.
4. Before every handoff/end of a development session, append a dated log entry with task IDs, changes, commands/results, source state, blockers and the next exact action. Update this document even if the only progress was discovering a blocker. Never mark a task done merely because code exists or a historical receipt passed.
5. A blocker entry states the concrete missing dependency/data/decision, attempted alternatives, impact and next action. Do independent ready work while blocked. `DEFERRED` requires a stated scope decision; never quietly defer a required presentation gate.
6. Reopen affected `DONE` tasks when code/model/data/policy changes invalidate their evidence. Refresh counts only from the table; do not estimate completion percentages from elapsed days.
7. Keep this file below 500 lines. When the log grows, move older entries into a dated file under `docs/progress/` and link it here, retaining current status and the latest handoff. Do not add an automated tracker unless maintaining this single file actually becomes a problem.
8. Ruflo memory may store the plan path, deadline and latest handoff as a secondary index. Git-visible documentation is the source of truth and must work when Ruflo is unavailable. Do not auto-start token-consuming background workers.

Session log template:

```text
YYYY-MM-DD HH:MM Asia/Calcutta | worker/session | task IDs
Status changes:
Changes and source state:
Checks: command -> actual result -> evidence path/hash
Blockers/decisions:
Next exact action:
```

## Session log

### 2026-09-13 | Codex integration | PLAN

- User set the presentation to 20 days away and accepted any model/training approach that produces working detection. Derived target is October 3; model freeze September 26, build freeze September 30.
- Read all 44 pages of Context.pdf, 6 pages of the SIH idea deck, 7 teacher slides and 10 handbook pages by PDF text extraction. No PDF was edited. Two read-only reviewers inspected backend and validation/presentation gaps; integration alone wrote the plan and session entry points.
- Inspected source and discovered the ignored/untracked `app/models/schemas.py` release blocker. Recorded streaming/readiness/restart/data/model gaps without implementing them.
- Fresh checks: `python -m pytest -q -p no:cacheprovider` -> 12 passed, 1 optional model smoke skipped in 0.44s; `node tests/ui_smoke.cjs` -> passed; `node tests/microphone_smoke.cjs` -> passed. These do not establish live API, actual model or physical microphone readiness.
- Ruflo MCP tools were not exposed in this session. Used installed `@claude-flow/cli` 3.38.19 for memory search (no matching plan found), task routing and the documentation policy receipt. Policy outcome: allowed, legacy mode; receipt `sha256:e7dbb66c4b022567902a5b378f2cfd695da47046e273452c3f1d7b0b5a06f3a3`. This records documentation authority, not implementation/release approval.
- Saved a secondary context index at Ruflo `patterns/vigilvoice-final-presentation-plan`. A read-only final plan review checked dates, model flexibility and progress rules; clarified first-alert latency denominators so misses remain visible without accidentally changing the detection target.
- Plan validation: all 10 local Markdown links resolve; 23 unique task IDs, only PLAN marked DONE; document below 500 lines; deadline arithmetic verified; `git diff --check` passed. Application/source/test diffs remain empty. Ruflo context index was retrieved successfully after saving.
- Existing configuration/runtime changes were preserved. No application code, model, threshold, dataset, database, deployment or source-control publication was changed by this planning task.
- Next exact action after implementation is requested: B01, correct the root model-artifact ignore scope and include the required schema module in a reviewable source snapshot; verify clean-source import/startup, then B02 and D01.

### 2026-09-13 12:55 Asia/Calcutta | Codex integration | B01, B02, D01

- Status changes: B01, B02 and D01 moved from pending/in-progress to DONE after their stated checks passed.
- Changes and source state: narrowed the root model ignore rule to `/models/`, exposing required `app/models/schemas.py` without exposing weights; added the B02 receipt and froze the four-language evaluation protocol with Marathi selected. Pre-existing Ruflo state changes were preserved. Baseline commit is `df2e83ee6249bfa0d056e169810152ba3870de43`; the required untracked schema and ignore-file hashes are recorded in the B02 receipt.
- Checks: clean source export -> 12 passed, 1 skipped; clean Python 3.11 image build/import -> passed; Compose rebuild/start -> passed while preserving the existing PostgreSQL container/volume; additive migration -> passed four times across pre/post rebuild; pinned model hashes -> passed; real-model unittest -> 5 passed; UI and microphone smoke -> passed; rebuilt-stack API/action suite -> all 13 groups passed; `git diff --check` -> passed. Evidence: [B02 receipt](validation/b02-baseline-2026-09-13.md), backend image `sha256:6b4e46cb7ff23cd134a9346b505c93614ec914da36e92b3229be21cd3425716a`.
- Blockers/decisions: host Python 3.13.5 with SQLAlchemy 2.0.30 cannot import SQLAlchemy, so Python 3.11 Docker remains the verified runtime. AI4Bharat data/model candidates require gated contact-sharing acceptance; no acceptance or download was performed. Team speakers/consent and a second permitted generator family are still missing for D02. Existing 40 English clips remain regression-only and cannot support final four-language claims.
- Next exact action: start B03 readiness/configuration validation in the existing backend paths while D02 obtains explicit consent and approved dataset/model access; do not tune thresholds before the frozen manifest is assembled.

### 2026-09-13 13:27 Asia/Calcutta | Codex integration + read-only swarm | B03

- Status changes: B03 moved from TODO through IN_PROGRESS to DONE after live success/failure checks and final review passed.
- Changes and source state: added centralized numeric settings validation; complete runtime-schema/database readiness; cached executable VAD/detector startup warmup; aggregate `/readyz` and `/api/v1/system` state; UI readiness and focused tests. The existing PostgreSQL volume and unrelated Ruflo state were preserved. Baseline, patch/test objects and rebuilt image are bound in the [B03 receipt](validation/b03-readiness-2026-09-13.md).
- Checks: 20 passed/1 skipped plus 22 validation subtests; Python 3.11 focused readiness 8 passed; UI/microphone smoke passed; rebuilt Compose stack ready; missing verifier/model/DB and invalid NaN configuration all failed closed; final 13-group live API/action suite passed; read-only re-review found no blocker.
- Blockers/decisions: Ruflo CLI routing failed before execution with npm `Invalid Version`; implementation continued using the Git ledger and a read-only analysis swarm. D02 still needs team consent, accepted gated access and a second permitted generator family. No data was downloaded or threshold tuned.
- Next exact action: B04, extend isolated API checks for database/audit rollback and restart-state denial/recovery, preserving existing demo records; continue consent/access work for D02 independently.

### 2026-09-13 14:02 Asia/Calcutta | Codex integration + Ruflo/read-only swarm | B04

- Status changes: B04 moved from TODO through IN_PROGRESS to DONE after its transaction-failure and hard-restart checks passed.
- Changes and source state: fixed startup handling for interrupted `LIVE`/`PROCESSING_FILE` sessions and extended `scripts/check_demo.py` with audit rollback plus two restart phases. Baseline is `5a5e62b`; the reviewable B04 patch, receipts and Ruflo state are uncommitted. Existing PostgreSQL records/volume were preserved.
- Checks: syntax and diff checks passed; focused Python 3.11 tests 15 passed/1 skipped; live API suite 14/14 passed; hard restart 5/5 passed; UI and microphone smoke passed; final `/readyz` returned 200. Exact hashes and limits are in the [B04 receipt](validation/b04-actions-2026-09-13.md).
- Blockers/decisions: Ruflo v3.35.0 core memory/routing/swarm paths now work; latest-package resolution remains unnecessary because only optional doctor warnings remain. The runtime image omits pytest, so explicit unittest files were run; dependency/test packaging remains R01. D02 access/consent remains open.
- Next exact action: B05, use the restart harness to make lost/expired verifier recovery explicit and bound upload/auth/inference/concurrent-session waits without persisting plaintext verification codes.

### 2026-09-13 22:30 Asia/Calcutta | Codex integration + Ruflo/read-only swarm | B05

- Status changes: B05 moved from TODO through IN_PROGRESS to DONE after focused, live-capacity and hard-restart checks passed.
- Changes and source state: bounded PostgreSQL waits, upload reads, audio admission and inference; fixed WebSocket overload codes and fail-closed source replacement; made lost verifier delivery expire the old challenge/action; corrected operator/verifier recovery text and removed identity/probability-overclaiming reason labels. Baseline is `5a5e62b`; B04/B05 remain an uncommitted reviewable source state bound to image `sha256:a41f612317a99a5a8944ce66ade05ccb1dadfc7db9b68b5c906c5d53c9a59d37`.
- Checks: host pytest 23 passed/1 skipped plus 26 subtests; Python 3.11 focused unittest 11 passed; UI and microphone smoke passed; rebuilt live suite 15/15 passed; hard SIGKILL restart suite 5/5 passed; final readiness returned 200. Evidence: [B05 receipt](validation/b05-capacity-verifier-2026-09-13.md) and linked machine receipts.
- Blockers/decisions: capacity is intentionally one active audio session in one Uvicorn worker; sessions 2/3 fail promptly and were measured only as admission checks, not long-run capacity. The first restart rerun exposed and fixed a harness-only variable shadow, then both full suites passed on the final source. Ruflo stored `patterns/vigilvoice-b05-handoff`; immediate semantic search returned no result, so this Git ledger remains authoritative. D02 still needs consent/access and a second permitted generator family.
- Next exact action: S01, add native continuous browser PCM streaming using the existing WebSocket contract while retaining WAV upload as fallback; then S02 bounds stream freshness/backlog.

### 2026-09-13 23:12 Asia/Calcutta | Codex integration + Ruflo/read-only swarm | S01

- Status changes: S01 moved from TODO through IN_PROGRESS to DONE after contract, live backend and headless Chrome checks passed.
- Changes and source state: added native 16 kHz AudioWorklet capture with 200 ms PCM16 frames; same-origin authenticated WebSocket client waits for `ready`; live results reuse the risk display; stop/failure cleanup preserves WAV fallback. Repaired binary-first backend handling and stale `test_client.py`. Baseline is `b84593e`; S01 is an uncommitted patch bound to image `sha256:f636ae6f920d3c8ac27cd26c1868d3c65fc6247298bff08390ce670c41207aef`.
- Checks: pytest 23 passed/1 skipped plus 26 subtests; microphone and UI smoke passed; live backend suite 15/15 passed; repaired client streamed 24 real-WAV frames; Chrome fake device produced eight updates while capture stayed ready, reached LOW at 1.5s, then passed WAV fallback; final readiness returned 200. Evidence: [S01 receipt](validation/s01-browser-streaming-2026-09-13.md).
- Blockers/decisions: S01 uses browser-native resampling and adds no dependency or reconnect queue. Fake-device browser proof is not physical microphone evidence. Backpressure, server-owned timing, delayed/replayed-frame handling and retained HIGH evidence remain S02; D02 access/consent remains open.
- Next exact action: S02, add the minimum bounded-frame and server-owned freshness protocol to the existing stream, then test stale/gapped input and genuine-to-synthetic-to-genuine action blocking.

### 2026-09-23 19:37 Asia/Calcutta | Codex integration | documentation

- Status changes: none. S02 remains the next development task.
- Changes and source state: added the root README with current setup, workflow, checks and measured limits. S01 is now in commit `b8d7375`; this documentation change is uncommitted. Existing Ruflo policy state was left untouched.
- Checks: README commands, routes and claims checked against application code, Compose, model setup and S01 receipt; `git diff --check` passed before this log update.
- Blockers/decisions: no new development evidence. Streaming freshness, representative evaluation and physical rehearsals remain open.
- Next exact action: implement S02 against the existing 200 ms stream, then test stale/gapped frames and action blocking during changing risk.

### 2026-09-23 19:43 Asia/Calcutta | Codex integration | documentation

- Status changes: none.
- Changes and source state: clarified the README's WAV upload format, byte and duration limits, speech requirements and an FFmpeg conversion example. No application behavior changed.
- Checks: format and limits checked against `evaluate_wav`, `MAX_AUDIO_BYTES`, the quality policy and browser recording encoder; documentation diff checked for whitespace errors.
- Blockers/decisions: S02 and evaluation work remain open.
- Next exact action: implement S02 stream freshness and backlog controls.

### 2026-09-24 05:55 Asia/Calcutta | Codex integration | S02, J01-J03, D02, M01-M02, V02-V03

- Status changes: S02 is DONE after final-source checks. J01 remains IN_PROGRESS until a real permitted Jev shadow response validates the external contract; J02 and D02 remain BLOCKED on external access/data. J03 stays DEFERRED. M01-M02 and V02-V03 remain IN_PROGRESS; no model replacement or threshold promotion was justified.
- Changes and source state: preserved the pre-existing README, Ruflo state and supplied papers. Added sequence/freshness/backpressure to the existing stream; fixed retained HIGH and disconnect/source-replacement action safety, evidence provenance and restart invalidation; added optional metadata-only Jev, audit and fail-safe modes; added dataset manifest/leakage checks and Stage 0/Jev evaluators. See [architecture/research assessment](validation/current-state-and-research-2026-09-24.md), [dataset matrix](dataset-acquisition.md), [manifest contract](manifest-schema.md), [Jev design](jev-integration.md), [Stage 0](validation/stage0-2026-09-24.md) and [S02 receipt](validation/s02-2026-09-24.md). HEAD `58f7e32` plus a dirty worktree is not an immutable release identity; final S02 image and changed-file hashes are in its receipt. No commit, push, gated-data acceptance, bulk download, external audio upload or existing demo-volume reset occurred.
- Checks: final source `python -m pytest -q -p no:cacheprovider` -> 29 passed, 1 skipped, 26 subtests; both Node smoke suites and Compose config -> passed; isolated Python 3.11 image -> ready, 19/19 API/action cases, 5/5 hard SIGKILL restart, 150 sequenced frames/30.4 s. Earlier S02 image -> 2,999 frames/600.19 s without protocol close, response median 11.17 ms/p95 392.69 ms and sampled backend memory near 778 MiB; not a final-image ten-minute claim. Fake-device Chrome stream/WAV fallback passed on that earlier image. Stage 0 on 40 historical English clips missed frozen targets and is not an untouched final test. Twenty scripted Jev policy scenarios yielded zero unsafe downgrades but no live-service agreement or latency. The first immediate live check after a new container was connection-refused before startup; retry after `/readyz` 200 passed. `git diff --check` passed with CRLF conversion warnings.
- Blockers/decisions: representative four-language, source/speaker/generator-disjoint permitted audio and consented Hinglish are unavailable; the existing 40 clips cannot select a defensible replacement. No Jev key or labeled live shadow corpus is configured, so advisory is not approved. Physical microphone, 10-second WAV latency distribution, final-image ten-minute run, full browser-to-action rehearsal, safe packaging and untouched final test remain open. Keep the local deployed detector and deterministic/offline path; keep Jev in shadow/no-key fallback or disabled.
- Next exact action: acquire approved source/target lineage and consent for D02, validate the rich manifest, run representative Stage 0 without changing the held-out test, then decide whether a frozen SSL head is warranted. Independently run physical microphone/action verification and R01 packaging on an exact reviewable source snapshot before presentation rehearsal.

### 2026-09-24 06:03 Asia/Calcutta | Codex integration | R01

- Status changes: R01 remains IN_PROGRESS; dependency/context and local no-egress runtime checks passed, but its source/export and preserved-db gates have not.
- Changes and source state: added a strict Docker context allowlist, pinned the Python base by digest and all 28 tested Python 3.11 packages, and removed the unused compiler install. The [R01 checkpoint](validation/r01-packaging-2026-09-24.md) binds the new image and file hashes. The R01-only test containers/internal network were removed after checks; the S02 test stack and existing demo stack remain. Model weights and the existing demo database volume were not changed; worktree changes remain uncommitted.
- Checks: `docker build -t vigilvoice-validation:r01 .` -> passed; image imports and `pip check` -> passed; image `/app` inventory contained only intended paths and no private files; isolated backend `/readyz` 200 and API/action 19/19; second backend on Docker `--internal` network `/readyz` 200, API/action 19/19 in 35.966 s, external TCP connect -> network unreachable. The first readiness request again preceded warm startup and was retried after readiness.
- Blockers/decisions: build still fetches pinned wheels and lacks wheel hashes; offline runtime requires a preloaded image, local models and PostgreSQL. A clean-clone/source-export test of this dirty tree, immutable handoff, and preserved-db upgrade rehearsal remain open. Do not treat the checkpoint as release evidence.
- Next exact action: prepare a reviewable snapshot including tracked and required untracked files, verify that source builds without private inputs, then test offline image import and preserved test-database restart. D02 remains blocked on permitted disjoint data; physical rehearsals remain open.

### 2026-09-24 10:06 Asia/Calcutta | Codex integration | R01

- Status changes: R01 remains IN_PROGRESS. Its dirty-source export, local offline archives, no-egress runtime and preserved isolated-test-database restart now have receipts; pristine-host Compose and final-source freeze remain unverified.
- Changes and source state: built a 42-file runtime-source archive including untracked Jev/manifest code and a 44-file offline source archive including Compose/README. Added an explicit default backend image tag to Compose, kept the build path for normal startup, and saved separate backend, PostgreSQL and local-model/public-demo archives. Large image/model archives are ignored by Git; no credentials or private audio were bundled. [Package manifest and commands](validation/offline-package-2026-09-24.md) and [R01 receipt](validation/r01-packaging-2026-09-24.md) bind their hashes. The superseded test-tag Docker tar was removed; the verified `vigilvoice:local` archive remains. No commit, push or existing demo-stack mutation occurred.
- Checks: extracted runtime source built; original/exported images had 40 identical `/app` files (combined SHA256 `bb98d4a2ee471c355031dbec6b1878c8dfe3221020b1be68fc964fdcef03cb8e`) despite differing image IDs. The extracted backend passed `/readyz` and 5/5 hard-SIGKILL restart checks against preserved isolated test records. Backend and PostgreSQL tar files reloaded into the same daemon with expected IDs. Extracted source+model ZIPs yielded valid Compose image selection and matched both ONNX and both public WAV hashes. Earlier R01 no-egress runtime suite remained 19/19. Exact artifact sizes/hashes are in the package manifest.
- Blockers/decisions: same-daemon `docker load` does not prove a fresh host; exact Compose startup from package and physical microphone are untested. Rebuilding still needs a package index because wheels are not bundled/hashed, though runtime can use preloaded images. The source snapshot is a checkpoint of a dirty tree, not the final release. D02 and live Jev evaluation still need permitted external inputs.
- Next exact action: S03/V01 browser-to-action and disconnect UI checks on an isolated backend, followed by physical microphone timing and a clean-host package rehearsal before the final freeze. Do not promote the model or Jev advisory mode without their blocked validation gates.

### 2026-09-24 11:15 Asia/Calcutta | Codex integration | S03, V01-V03, R01

- Status changes: S03 and V01 moved to DONE after their fake-device browser and action-bound checks passed. V02, V03 and R01 remain IN_PROGRESS; D02/J02 remain BLOCKED on external data/service access, and J03 remains DEFERRED.
- Changes and source state: added server-owned evidence age and bounded dashboard refresh, cleaned stale risk/action display on stream stop, tested permission denial/pagehide, and corrected stream admission before source mutation. A red/green V02 regression then exposed and fixed the same ordering defect in WAV upload: a capacity-rejected upload no longer replaces an admitted live generation. Final isolated image `sha256:136591533727ec788a0e6365adf395ceefe562c3e856637b8ec36efa0f51c2d4`; source and browser hashes are in the [S03/V01 receipt](validation/s03-2026-09-24.md). No model/threshold change, commit, push, secret publication or original demo-stack mutation occurred.
- Checks: final-image Python 30 passed/1 skipped plus 26 subtests; both Node smoke suites passed. The updated live API/action suite passed 20/20, the Chrome fake-device/browser action flow passed 15 checks, and hard-SIGKILL restart recovery passed 5/5. Browser proof includes real genuine action completion, same-stream synthetic HIGH blocking a VERIFIED action with direct completion denied and later LOW unable to revive it, source-replacement blocking, and synthetic WAV blocking. See the [browser report](validation/s03-action-browser-2026-09-24/browser-checks.json). The [V03 checkpoint](validation/v03-2026-09-24.md) records 12 warmed 10-second WAVs (median 5.19 s, p95 5.26 s), 2,999 frames/600.2 s on the final image with median/p95 response 11.83/400.01 ms, sampled backend memory around 784 MiB, one synthetic clip's first HIGH at 2.4 audio seconds, and warm-host restart-to-ready 13.31 s. Readiness remained 200.
- Packaging: the [updated offline manifest](validation/offline-package-2026-09-24.md) binds a new 44-file source ZIP SHA256 `0e07a0cc96536161b3594c861bae9776cebaf33ed7fde7e46541f1ab39eb722e` and final-image tar SHA256 `212ecb9fc6662640189ece3894921241029022e1c1955ef49f357147c5cde9fb`; the unchanged model/PostgreSQL archives remain separate. Extracted source matched all staged hashes, Compose config validated with placeholder credentials, same-daemon image load matched the ID, and the exact image passed 20/20 on a Docker internal-only network with external TCP blocked. The temporary offline container/network were removed; isolated test records and original demo stack were preserved. Unsuffixed earlier source/backend archives are superseded, not deleted.
- Limitations: Chrome used a fake input file, not a physical microphone/OS permission interaction. The repeated public WAV and one known synthetic clip do not establish multilingual accuracy or first-alert distribution. No live Jev access, permitted four-language corpus, pristine-host Compose rehearsal, final model freeze or untouched final test exists. The local Ruflo security-scan CLI was unavailable without downloading a package; a proposed DB-outage drill was rejected by the command runner before it altered the test DB, so that failure path remains unverified. `git diff --check` passed with only LF/CRLF conversion warnings; worktree remains intentionally dirty.
- Next exact action: collect consented, source-disjoint four-language material for Stage 0 and obtain a permitted Jev shadow key if desired. Independently rehearse the physical microphone/verifier path on the presentation laptop, run an authorized isolated DB-outage check, then perform pristine-host offline Compose startup and final-source freeze. Keep Jev non-advisory and defer model replacement until validation gates pass.

### 2026-09-24 11:40 Asia/Calcutta | Codex integration | V04, R01

- Status changes: V04 moved to DONE for the local presentation security review; R01 remains IN_PROGRESS because its `v03` archives predate this source change. V02/V03, D02/J02 and the final release gates remain open as stated above.
- Changes and source state: added a 60-per-rolling-minute one-process session-creation cap and an eight-slot pre-auth WebSocket cap; slots are released after credential checking, before admitted audio. Updated the existing capacity test and live API harness, README and [V04 receipt](validation/v04-2026-09-24.md). Isolated final image `sha256:b2543871eefbf2ff2461fb45c15e6f23339f99aa85dd84e0245813b124eee04f`; HEAD `58f7e32` plus dirty tracked/untracked files is not an immutable release identity. Existing demo stack and database were untouched; no commit or push.
- Checks: Python 31 passed/1 skipped plus 26 subtests; both Node smoke suites passed; isolated backend 21/21 API groups, including ninth silent WebSocket rejection, and fake-device Chrome 15/15 action-flow checks with zero unauthorized completions. Live session admission made 60 sessions then rejected five 429s; `Retry-After: 60` observed. `uvx pip-audit -r requirements.txt --no-deps --disable-pip` reported no known advisories for pinned packages, with an unhashed-dependency warning. Initial API run before v04 readiness failed connection-refused; rerun after startup passed. Exact hashes and limits are in the V04 receipt.
- Limitations and next exact action: rate/session caps are per worker; no distributed DoS control, image/OS audit or real-data retention policy. The verifier remains a local role; physical mic, representative data, live Jev, DB-outage, pristine-host offline launch and final freeze still lack evidence. Next, perform the remaining V02 isolated DB-failure check or physical-device V03 rehearsal when available; refresh R01 archives only after the final source stabilizes. Keep Jev shadow-only and do not claim model accuracy from the English demo clips.

### 2026-09-24 11:59 Asia/Calcutta | Codex reference review | J02, M01-M02

- Status changes: none. Reviewed three user-supplied public repositories at the pinned commits in the [reference review](#reference-repositories-reviewed-september-24); added only conditional evaluation details to J02 and M01. Implementation remains paused pending user approval.
- Evidence and decision: MarkHershey's feature/generator comparison is useful as a baseline idea but its README uses test data for model selection; preserve VigilVoice's untouched test. Guri10's LIME/SHAP/gradient examples are image-model notebooks without verified streaming/generalization or an identified code license; do not import them. The Jev catalog suggests offline typed-answer replay and calibration, but is a directory, not a safety validation. No external code, models, data, dependencies or credentials were brought into VigilVoice, and no Jev API call was made. The working tree was clean at `6418c1c` before this plan-only edit.
- Blockers and next exact action: D02 corpus/rights, J02 live key and labels, physical microphone access, V02 isolated DB-failure check and final freeze remain open. After approval, start with the safe isolated V02 failure check and M04 quality diagnostics, then run D02/M02 and J02 only when their permitted inputs exist. Do not replace the detector or enable advisory based on these references alone.

### 2026-09-24 12:27 Asia/Calcutta | Codex integration | V02, M04

- Status changes: M04 moved to IN_PROGRESS. V02 remains IN_PROGRESS: injected database commit failure is covered, but no real outage was run. D02/J02 and final-source release gates remain open.
- Changes and source state: added an isolated commit-failure rollback/retry case to the existing API harness and a readiness-error redaction assertion. Added a diagnostic-only public-audio quality probe. [V02 receipt](validation/v02-commit-failure-2026-09-24.md) and [M04 receipt](validation/m04-public-probe-2026-09-24.md) bind reports and hashes. No application policy, model, threshold, source audio, Jev mode, demo database or release artifact changed; no commit or push.
- Checks: isolated backend API/action harness passed 22/22 with injected commit rollback, unchanged VERIFIED/unconsumed approval, zero completion audit rows and successful legitimate retry; readiness error returned 503 without the injected private detail. Python tests passed 31, skipped 1, with 26 subtests; both Node browser smoke suites passed. The first 22/22 harness run hung during Python shutdown after writing its passing report and its test-only process was terminated; a second instrumented run exited 0 in 39.808 s. This does not establish actual PostgreSQL-outage behavior.
- M04 finding: 17 probe rows on the two public demo WAVs. The clean synthetic clip scored HIGH (.756581) but LOW with nominal 10 dB additive noise (.113638) or simulated 300-3400 Hz band-limit (.135760). Both short 0.5/1-second prefixes lacked sufficient speech. No detector or quality-policy adjustment is justified from two clips; the simulated filter is not a real telephone channel. The VAD-derived SNR fallback may inflate all-voiced noisy windows and needs representative evidence before changing.
- Blockers and next exact action: obtain rights-cleared, speaker/source/generator-disjoint Hindi, Indian English, Hinglish and Marathi/Punjabi audio for D02 and run representative Stage 0. Arrange a physical-microphone V03 rehearsal and an authorized isolated PostgreSQL-outage V02 check. Keep Jev shadow/no-key fallback, preserve action-bound OTP and HIGH retention, and refresh R01 archives only after source freeze.

### 2026-09-24 12:54 Asia/Calcutta | Codex integration | R02 documentation checkpoint

- Status changes: R02 moved to IN_PROGRESS; final deck/handbook, narrated backup video, final-source evidence and physical rehearsal still prevent DONE.
- Changes and evidence: updated the existing [presentation guide](teacher-demo-guide.md) and [prototype runbook](prototype-runbook.md) in place. They now describe continuous sequenced streaming, WAV fallback, local verifier limitations, six rehearsal cases, HIGH retention, the M04 synthetic misses, offline caveat and exact current-stage Q&A. The old PDF/video are explicitly labeled historical. No application code, model, threshold, API, database, Jev mode, private audio, credentials or release artifact was changed by this checkpoint.
- Checks: documentation claims checked against browser labels/flow, current config TTLs, S03/V01 browser receipt, V03 timing receipt, Stage 0 and M04 reports. Ten referenced local files exist. `git diff --check` passed with LF/CRLF conversion warnings. No browser/hardware rehearsal or PDF/video visual review was performed for this documentation-only change.
- Next exact action: obtain permitted D02 data for representative Stage 0 and physical-microphone presentation time. Before R02 completion, revise the deck/handbook and record a current-build HIGH/BLOCKED narrated fallback after exact-source freeze; visually review every final PDF page and run three timed physical/offline rehearsals. Do not use the historical video as current-build proof.

### 2026-09-24 12:59 Asia/Calcutta | Codex integration | D02 metadata, R02 documentation

- Status changes: no new DONE gate. D02 remains BLOCKED on access, consent and independent lineage; R02 remains IN_PROGRESS.
- Evidence and changes: rechecked the official IndicSynth, IndicVoices, IndicVoices-R and WaveFake records without accepting gates or downloading audio. Corrected the [acquisition matrix](dataset-acquisition.md): WaveFake v1.2 distributes synthetic clips but not genuine reference audio. IndicSynth remains CC BY-NC with source/target identifiers; both IndicVoices variants still require login/contact-sharing acceptance. Updated the [evaluation guide](evaluation-guide.md) to distinguish its legacy five-column English regression manifest from the validated rich schema/held-out-family path. Added the M04 detector miss to the README limitations and an explicit Jev-disabled offline rehearsal instruction to the presentation guide.
- Checks: `python -m pytest -q -p no:cacheprovider` passed 31/1 skipped/26 subtests; UI and microphone Node smoke suites passed. Official dataset cards/record were inspected, not treated as acquisition approval. No application behavior, dataset split, model, threshold, credentials or media artifact changed. The new documentation links and `git diff --check` still need final review after this handoff edit.
- Next exact action: secure permitted source-linked audio and consent or explicitly defer model replacement at the September 26 checkpoint; arrange physical microphone/isolated outage rehearsal. Keep the historical deck/video labeled as such until a final-source replacement is visually checked.

### 2026-09-24 13:12 Asia/Calcutta | Codex integration | M01 evaluator safety

- Status changes: M01 remains IN_PROGRESS. The richer evaluator no longer exposes intended final-test model scores during validation; no representative corpus or final test exists.
- Cause and change: the rich-manifest CLI previously inferred on validation and test together before choosing its validation-only threshold. Added explicit `--phase validation` and `--phase final-test`; the latter requires a supplied finite frozen HIGH threshold. Reports contain only the selected phase's metrics and slices. The historical five-column exploratory command remains combined for reproducibility. Updated [manifest](manifest-schema.md) and [evaluation](evaluation-guide.md) instructions; [M01 receipt](validation/m01-phase-separation-2026-09-24.md) binds the exact source hashes and limits. No deployed policy/model/threshold, dataset split or final-test audio changed.
- Checks: focused evaluator/manifest tests 4/4, final Python 32 passed/1 skipped plus 26 subtests, UI and microphone Node smoke suites passed. Mocked evaluator-path tests proved split isolation and legacy behavior; no approved rich corpus was scored. The validator still reads test files only for integrity/leakage checks, not inference. The CLI cannot enforce an external model/policy freeze or prevent a user rerunning a final test.
- Next exact action: obtain D02 lineage/consent and validate a representative rich manifest; run validation only, record a source/model/policy freeze, then run the untouched test once. If D02 remains unavailable by September 26, defer model replacement and disclose the unmet target rather than tuning on demo or historical clips.

### 2026-09-24 13:16 Asia/Calcutta | Codex integration | M04 Docker-runtime confirmation

- Status changes: M04 remains IN_PROGRESS. No detector, threshold or quality-rule change is justified.
- Evidence: reran the same 17-condition public-clip probe inside the isolated `vv-v04-backend` using Python 3.11.16 and ONNX Runtime 1.30.0 with the same mounted models. All 17 risk states matched host Python 3.13.5/ONNX Runtime 1.26.0, though nine numeric scores differed slightly. Docker synthetic clean HIGH 0.756676 became LOW 0.113789 with nominal 10 dB noise and LOW 0.134800 after simulated band-limiting. [M04 receipt and Docker JSON](validation/m04-public-probe-2026-09-24.md) bind the run. No private or gated audio was used; the existing demo database was not touched.
- Final source review: `git diff --check` exited 0 with only LF/CRLF conversion warnings. The worktree remains intentionally dirty at HEAD `6418c1c`; prior Ruflo policy state and all user-owned edits were preserved. No commit or push.
- Next exact action: obtain representative genuine/synthetic noisy and real phone-channel recordings with source linkage, then compare quality/detection rules on validation only. Keep the known synthetic miss visible in the presentation limitations and do not adjust a threshold using two public demo clips.

### 2026-09-24 17:54 Asia/Calcutta | Codex-assisted solo rehearsal | V03, R02

- Status changes: none. The user reported real laptop microphone capture and ultimately completed one simulated transfer using the local verifier role. This is a partial rehearsal, not independent verification or current-build V03/R02 acceptance.
- Persisted evidence: the newest action at 08:50:53 UTC was `COMPLETED` at `ELEVATED` risk; its challenge was `VERIFIED` with zero failed attempts, the approval was consumed, and the action audit contains `ACTION_CREATED`, `OTP_CHALLENGE_ISSUED`, `OTP_VERIFIED` and `ACTION_COMPLETED`. Read-only queries inspected status/event types, not OTP, recipient or raw audio. Earlier attempts exposed stream disconnects, actions correctly blocked at creation when evidence was unavailable, and three WAV uploads that each ended in `INFERENCE_FAILED_OR_TIMED_OUT` after about 10 seconds.
- Source limit: the running `vigilvoice_backend` image is `sha256:fbd7c1cde1f7ff17a6d980d5fc6b4b455b3b42f9efbade7e3bffb7f3f23a9532`, started September 23. Its browser and stream source differ from HEAD `4f832ea`; it lacks later sequenced/backpressure and pending-action-disconnect changes. Browser control was unavailable, so the physical microphone and operator steps were user-reported rather than independently observed. The same person operated the verifier. Do not call this a final-build or separate-person rehearsal.
- Checks and next exact action: Docker `/readyz` was 200, and the database action/challenge/approval/audit checks above passed. No app, model, threshold, configuration, database row or secret was changed by this documentation update. After the user permits interrupting the demo, rebuild the current checkout without deleting the PostgreSQL volume, then repeat physical-microphone, WAV fallback and action-bound verification checks. Retain V02 isolated DB-outage, D02 representative corpus and final-source release gates.

### 2026-09-24 18:02 Asia/Calcutta | Codex integration | V02 isolated database outage

- Status: V02 remains IN_PROGRESS. The isolated `vv-s02-net` contained only `vv-v04-backend` (image `sha256:b2543871eefbf2ff2461fb45c15e6f23339f99aa85dd84e0245813b124eee04f`) and `vv-s02-db`; neither live demo container nor its volume was touched. A test-only action used the bundled public genuine WAV and was PENDING before the outage.
- Observation: `vv-s02-db` was paused only within a `try/finally` and confirmed running/unpaused afterward. `/readyz` and a verification request produced no client response within eight seconds. The isolated backend logged an unhandled `TimeoutError` in `request_verification`. After recovery, the test action remained PENDING with zero `OTP_CHALLENGE_ISSUED` and zero `ACTION_COMPLETED` events. No approval token or private recording was involved. This supports fail-closed outcome for the exercised request, but fails bounded error-response acceptance.
- Minimal change: registered a generic `TimeoutError` HTTP handler in `app/main.py` returning a redacted 503. Extended the existing `tests/test_readiness.py` check to assert handler registration, 503 and secret redaction. The red test failed before the handler; the focused suite then passed 8/8 plus 26 subtests. Full Python passed 32/1 skipped plus 26 subtests; both Node UI/microphone smoke suites passed. `git diff --check` exited 0 with only line-ending warnings. HEAD `4f832ea`; the unrelated Ruflo policy state remained untouched. No model, threshold, OTP, action policy or live demo service was changed.
- Limit and next exact action: the changed source was **not** built into the isolated backend, so real outage response status/latency and recovery are unverified. Build an isolated current-source image, repeat the outage with a bounded client and verify 503, zero challenge/completion, no leaked details and successful recovery. Do not mark V02 DONE or promote this to the presentation image based on a unit handler test.

### 2026-09-24 18:07 Asia/Calcutta | Codex integration | local demo rebuild, V03/R02 pending retest

- User authorization: after the solo rehearsal, the user permitted a recommended rebuild/restart. Built `vigilvoice:local` from the current checkout before touching the running demo. The resulting image is `sha256:3de5e973adeb3c57b808ba4a471d52d5d7746e0fd7b163201cbc1694449bf2c0`; SHA256 of `app/main.py`, `app/api/v1/stream.py` and `app/services/audio_evidence.py` inside the image matched the local files. Its context excluded `.env`, tests and models; models remain a read-only mount. The image includes the new generic timeout handler, but its real-outage behavior is not yet proven.
- Restart and preservation: `docker compose up -d --no-build --no-deps backend` recreated only `vigilvoice_backend`; `vigilvoice_db` stayed running with named volume `sih_postgres_data`. Additive migration completed, `/readyz` returned 200 with DB, VAD, detector and demo verifier available. Before and after counts were identical: 84 sessions, 322 actions and 2,884 audit rows. No `down`, volume removal, model change or secret disclosure occurred.
- Checks and limitations: local Python passed 32/1 skipped plus 26 subtests, both Node UI/microphone smoke suites passed and `git diff --check` had only line-ending warnings. HEAD `4f832ea` plus tracked working changes is not a frozen release identity. The old browser tab must hard-refresh to load the sequenced stream client; restart invalidated live evidence and the in-memory verifier inbox, so a new action/code is required. User-operated physical-microphone and action-bound verification on this rebuilt image are still pending, as are isolated current-image DB-outage, offline artifact refresh, D02 and final freeze.
- Next exact action: collect the user's post-refresh physical-mic and new-action result without collecting private audio, OTP or recipient; then run the isolated current-image outage response check. Do not call V03/R02 or V02 DONE from rebuild/readiness alone.

### 2026-09-25 11:10 Asia/Calcutta | Codex integration | current-build physical retest, WAV fallback, V02 outage

- Status changes: none. V02/V03/R02 remain IN_PROGRESS; D02/J02 remain blocked and Jev remains shadow-only. No detector threshold, evidence TTL or authorization rule was changed.
- Physical-microphone evidence: the user reported a stable live connection for over a minute, deliberately stopped it, and saw LOW, ELEVATED and HIGH during their own speech. The rebuilt demo's persisted audit shows one action created at HIGH and blocked, an ELEVATED action subsequently blocked by `HIGH_EVIDENCE_DURING_ACTION`, and a LOW action with an issued challenge subsequently blocked at verification by `POOR_QUALITY`. Nineteen HIGH evaluations in that session scored 0.783-0.911 with 1.5-2.0 seconds of detected speech; they were `HIGH_SPOOF_SCORE`, not quality rejections. The rolling evidence moved from LOW at 05:16:04 UTC to insufficient speech and then `AUDIO_TOO_QUIET` from 05:16:06; the block was at 05:16:21. No action in that session completed. The Jev `missing_key` shadow event recorded the creation-time `PENDING` decision, not a later approval or final action status. These repeated windows are not independent accuracy samples, and the user's speech alone does not establish a false-positive rate.
- Safe fallback check: a new backend on the isolated `vv-s02-net` used current `vigilvoice:local` image `sha256:3de5e973adeb3c57b808ba4a471d52d5d7746e0fd7b163201cbc1694449bf2c0` and the existing test-only database. Its `/readyz` returned 200. The bundled public `models/demo/genuine.wav` yielded ELEVATED, then the test action went PENDING -> verified -> COMPLETED through the local verifier. No private audio or code was printed or saved. This proves an isolated API path, not the physical-mic browser WAV workflow.
- V02 current-image outage: a separate PENDING public-WAV action was prepared before pausing only `vv-s02-db` inside a `try/finally`. Verification gave the client no response within eight seconds (`TaskCanceledException`); after unpause, the database was running, readiness returned 200 in 0.31 seconds, and the action remained PENDING with zero challenge rows, issued events and completion events. The generic `TimeoutError` handler does not by itself bound this request. Do not mark V02 DONE or claim a 503 response until a real outage retest passes.
- Timeout trace: a separate established SQLAlchemy/asyncpg session on the same isolated backend executed `SELECT 1`, waited 15 seconds, then issued another `SELECT 1` while `vv-s02-db` was paused. Despite `DATABASE_TIMEOUT_SECONDS=2`, that query did not return while the database was paused; it raised `TimeoutError` only after unpause, 12.91 seconds after execution began, and session cleanup then completed. The isolated DB was confirmed running/unpaused. This narrows the failure to an in-flight database wait rather than model loading or Jev, but does not yet prove how every request path handles cancellation. Do not add an outer response timeout that could allow a late challenge/commit without a transaction-level cancellation proof.
- A second isolated established-connection probe wrapped the paused-DB `SELECT 1` in `asyncio.wait_for(..., timeout=2)`. It too returned `TimeoutError` only after unpause, 12.62 seconds after execution began; session cleanup then completed. A simple application-level `wait_for` therefore does not establish bounded response under this injected pause. The isolated DB was unpaused and the temporary backend stopped again; no action or demo database was touched by this probe.
- Current-checkout checks: `python -m pytest -q -p no:cacheprovider` passed 32/1 skipped/26 subtests; both Node UI/microphone smoke suites passed; `git diff --check` exited 0 with only line-ending warnings. The new isolated backend container was stopped after the tests; the test database remained running/unpaused and the presentation stack was untouched. HEAD `4f832ea` plus preserved working changes is not a release identity. This session changed only this plan document; no app code, policy, model, secret, commit or push. These unit/smoke checks do not override the observed outage failure or establish physical-WAV browser completion.
- Next exact action: diagnose the database wait/cleanup boundary on the isolated backend, then add the smallest bounded fail-closed fix with a red/green outage test. For presentation, rehearse the already-documented recorded-WAV path with the physical microphone and separate verifier role. Preserve live HIGH retention and source-disconnect blocking. At the September 26 checkpoint, defer detector replacement if permitted disjoint data remains unavailable; disclose the unvalidated genuine HIGH spikes and synthetic-noise misses.

### 2026-09-25 12:41 Asia/Calcutta | Codex-assisted current-build WAV rehearsal | V03, R02

- Status changes: none. The user reports recording with the laptop microphone, analyzing the WAV and completing a LOW-risk simulated transfer on the rebuilt presentation backend. The persisted session is `FILE_READY` with LOW, two seconds of detected speech, model `wav2vec2-xlsr-int8-4b1c4a294ab6:sha256:7af799e8443c3d030bb3ef644ffc6e74f5b6fddcd6a4286ee9abe9e828cb762e` and threshold profile `prototype-uncalibrated-v1:9535b80951b0`.
- Read-only database verification: the action created at 07:08:06 UTC is `COMPLETED` at LOW at 07:08:28; challenge status is VERIFIED with zero failed attempts, approval consumed, and exactly one `ACTION_COMPLETED` audit event. The action audit sequence is `ACTION_CREATED`, `JEV_DECISION` (`missing_key` in shadow), `OTP_CHALLENGE_ISSUED`, `OTP_VERIFIED`, `ACTION_COMPLETED`. The Jev event recorded deterministic/final PENDING at creation; it did not approve or complete the action. No code, recipient, raw audio, credential or hash was read or recorded for this check.
- Limits and next action: the database confirms the file-analysis/action path, while physical capture and operator steps are user-reported. This was not a separately staffed verifier rehearsal, a synthetic HIGH demonstration, a model-accuracy estimate or final-source acceptance. Arrange a second person for the verifier role and run the six-case/offline rehearsals; complete final deck/handbook/video and D02/M03 gates or disclose them. Keep V02 bounded-outage behavior open. This session changed only the plan ledger; no application code, model, threshold, credential, commit or push.

### 2026-09-25 16:17 Asia/Calcutta | Codex integration | V02 bounded outage, M01 selection

- Status: V02 and M01 remain IN_PROGRESS; no D02 corpus, model/threshold freeze or final-test inference. The presentation demo and its database were not paused/rebuilt in this session; Jev remains shadow/no-key.
- V02: a SQLAlchemy `handle_error` hook terminates only the timed-out/cancelled asyncpg connection, avoiding the observed paused-server cancellation wait. The 503 message now says to check the action status before retrying, because an isolated pre-COMMIT pause returned a client timeout even though its test-only BLOCKED update later committed. The exact-source isolated image `sha256:7e05b4b9ca2322786c1e9fdbbfd707b1eacaf9503b76f4c2783812deb3a6eb52` returned 503 in 2.15 s for PENDING verification during a DB pause; after unpause the action was PENDING with zero challenge/completion events. Its API suite passed 22/22 groups. See [V02 receipt](validation/v02-database-timeout-2026-09-25.md). A late-COMMIT verification/completion probe and frozen-release recheck remain.
- M01: rich validation now selects maximum synthetic recall under the pre-frozen <=10% scorable-genuine HIGH false-block bound, with exact-score candidates and no silent bound relaxation. Historical five-column selection remains unchanged. Stage 0 validation/test-only manifests are accepted; speaker IDs are compared across heard/source/target roles. Rich reports distinguish default from candidate slices. See [M01 receipt](validation/m01-selection-2026-09-25.md). No approved four-language corpus was scored.
- Checks: focused evaluator/manifest tests 5/5, full Python 34 passed/1 skipped/26 subtests, both Node smoke suites passed, `git diff --check` exited 0 with line-ending warnings, isolated exact-source readiness 200 and API 22/22. Image hashes of the four changed application/evaluation files matched local source. Both V02 test backends were stopped after validation; their separate test database remains running/unpaused, and the presentation stack remains running. The intentionally dirty worktree and Ruflo state were preserved; no commit, push, model, deployed threshold, secret, OTP, private recording or release archive was changed.
- Next: secure permitted D02 data or explicitly defer detector replacement at the September 26 checkpoint; then complete V02 late-COMMIT checks, R02 final truthful assets and separate-verifier rehearsal, and only afterward freeze/package the release source. An ambiguous 503 is not evidence of rollback.

### 2026-09-25 16:29 Asia/Calcutta | Codex integration | R02 current deck and handbook

- Status: R02 remains IN_PROGRESS. The existing seven-slide deck and ten-page handbook were updated and rendered as PDFs from their HTML sources; all 17 page previews were visually reviewed at desktop size. The old silent video is still historical; no narrated current-build backup was created. See [R02 asset receipt](validation/r02-assets-2026-09-25.md).
- Evidence: the browser checker used an isolated exact-source test backend with a fake microphone and passed its 11 UI checks. Its new screenshot is source-bound and explicitly labeled fake-device in the handbook. `pypdf` counted 7 deck and 10 handbook pages. No user recordings, OTP values, credentials or presentation database rows were used or exposed. The browser checker gained presentation-page previews and a desktop viewport reset; its browser smoke run passed after that change.
- Limits and next: re-render after the eventual model/policy/source freeze; record a truthful narrated HIGH/BLOCKED fallback, then conduct separate-person verifier and physically offline rehearsals. D02/M03 are still release blockers; no final acceptance or publication claim. Generated preview directories remain local and reproducible; an attempted cleanup command was rejected by policy, so no preview was deleted. They are now ignored to prevent accidental commit.

### 2026-09-25 16:40 Asia/Calcutta | Codex integration | V02 post-COMMIT retry semantics

- Status: V02 remains IN_PROGRESS, not a release pass. A separate [isolated-only check](../scripts/check_commit_ambiguity.py) injected a timeout after actual commit in the verification, OTP-confirmation and completion routes. All three passed with persisted state and audit checks; see the updated [V02 receipt](validation/v02-database-timeout-2026-09-25.md). The script refuses to run against the presentation database. One initial check failed because its dummy approval did not satisfy schema length validation; this was corrected and the whole three-case run passed. No secrets or OTP values were logged.
- Scope and remaining: this was an application-level post-COMMIT error injection, not a transport pause at the exact wire-level COMMIT boundary. It showed a committed-but-undelivered challenge cannot progress, a committed-but-unreturned approval cannot be used, and a committed completion is visible and non-replayable. The isolated backend was stopped afterward; the test database remained running/unpaused. Recheck on the frozen release image and keep ambiguous 503 guidance. D02/M03, R02 narrated video and separate-verifier/offline rehearsals remain open.

### 2026-09-25 16:48 Asia/Calcutta | Codex integration | R02 isolated browser/action proof

- Status: R02 remains IN_PROGRESS. The current-source isolated fake-device [browser/action receipt](validation/r02-action-browser-2026-09-25/browser-checks.json) passed 15 checks, including action-bound genuine completion, live HIGH retention after later LOW, source-replacement denial, synthetic HIGH/BLOCKED and zero unauthorized completions. The [HIGH/BLOCKED screenshot](validation/r02-action-browser-2026-09-25/teacher-demo.png) is available for a truthful static backup, but not as a physical-device or accuracy claim. No private mic recording, code or verifier key entered the receipt; the presentation database was untouched.
- Next: a narrated current-build video still requires source freeze and review; keep the silent historical video labeled as such. Separate-person and physically offline rehearsal, D02/M03 data gates and final release packaging remain. The isolated browser-test backend was stopped after the check.

### 2026-09-25 21:52 Asia/Calcutta | Codex integration | R01 source-docs checkpoint, V02 current-image recheck

- Status: R01 and V02 remain IN_PROGRESS; no model/policy freeze, final package or physical/offline rehearsal is claimed. The existing demo backend/database were not restarted or changed.
- Change: added a deterministic allowlisted source/doc ZIP command and one regression test. It includes all ten local README documentation targets, presentation PDFs and the historical labeled video; excludes `.env`, weights, private audio and previous archives. A current-source isolated image `sha256:5f79f4720ff12045116e8abde9c3124390c57854c48227be60e4d913eca759ed` used only `vv-s02-db` and read-only models. See [R01 checkpoint](validation/r01-packaging-2026-09-24.md).
- Checks: focused package test 1/1; full Python 35 passed/1 skipped/26 subtests; both Node smoke suites passed. The current-image API report recorded 22/22 passing groups and exited 0; all three post-COMMIT ambiguity checks passed. An initial API run with Jev disabled failed the harness's expected shadow-event assertion, then passed when the isolated backend used shadow/no key. The test backend remains isolated; no Jev service call was made. A real wire-level COMMIT pause and frozen-image recheck remain open.
- Next: generate and hash the checkpoint ZIP after this handoff, then verify its entry inventory and extraction/Compose configuration without touching the presentation stack. Source/model freeze, final images, independent offline host, D02 corpus, narrated video and separate-person rehearsal remain open. Do not promote the detector or Jev advisory from these tests.

### 2026-09-25 22:04 Asia/Calcutta | Codex integration | R01 offline, V03 timing, test harness

- Status: R01 and V03 remain IN_PROGRESS; no task was marked DONE from a checkpoint. D02/M03 model-validity and final release gates remain open.
- Change: the API harness now verifies the configured Jev `shadow` or `disabled` fallback, allowing the real offline path to be tested without weakening action assertions. The generated-artifact ignore file now excludes candidate ZIPs, extracted source and public test WAV from Git. No application service code, model, threshold, Jev advisory flag, presentation backend/database or private audio changed.
- Checks: focused Jev/packager tests 3/3; 20 scripted Jev scenarios remained policy fixtures. Current-source image `sha256:eac8e0aebca3003aa8fce4f4e8910d4d9d9f663ac0cec51d72990a15a617c189` on a Docker internal-only network with test database/local models and `JEV_MODE=disabled` reached `/readyz`, external TCP returned `ENETUNREACH` 101, and all 22 API groups passed in 42.068 s. Twelve repeated-public 10-second WAVs measured median 5,216.10 ms/p95 5,341.72 ms; a separate active upload sampled 297.19% CPU/825.5 MiB backend memory. See [R01](validation/r01-packaging-2026-09-24.md) and [V03](validation/v03-2026-09-24.md). These are not multilingual accuracy, physical-microphone timing or minimum-hardware claims.
- Validation-layout correction: an extracted source candidate under `docs/validation/artifacts/` caused pytest to discover a duplicate `scripts/test_client.py`; `pytest.ini` now excludes that generated-artifact directory. The full suite then passed again with the extracted copy present (35 passed/1 skipped/26 subtests). Future clean-host checks should extract outside the repository.
- Next: rebuild and hash the source/docs archive after this evidence update; ensure it remains separate from images/models. Then retain the no-egress and extracted-source receipts for a final frozen build. The current internal network/backend must be cleaned up without touching the presentation stack. Still needed: permitted corpus or explicit detector-replacement deferral, final model/policy freeze and untouched test if possible, pristine-host Compose/preserved-DB rehearsal, narrated final-build video and three separate-person/offline physical rehearsals. The video skill requires a final preview/approval before rendering, so no provisional screenshot is mislabeled a final-build narrated video.

### 2026-09-25 22:08 Asia/Calcutta | Codex integration | source ZIP Windows-path correction

- Status: R01 remains IN_PROGRESS. The first `%TEMP%` extraction self-check failed on a Windows short-path alias even though Docker built afterward; this is not counted as an extraction pass. The packager now normalizes the root before file/README-link comparison, and a regression test covers an equivalent root alias.
- Checks: exact extracted-folder self-check found 116 files, focused packager tests 2/2, full Python 36 passed/1 skipped/26 subtests, both Node smoke suites passed. No app runtime, model, threshold, authorization control or presentation stack changed. The internal-only test backend passed 22/22 earlier and remains test-only.
- Next: generate a new checkpoint ZIP from this exact source/doc state, verify extraction/Compose/build and record its hash outside the bundled tree. Do not call it the final release: D02/M03, final image/model freeze, pristine-host startup, video and separate-person/physical/offline rehearsals remain open.

### 2026-09-25 22:13 Asia/Calcutta | Codex integration | validated source ZIP checkpoint

- Status: R01, V02 and V03 remain IN_PROGRESS. No final release or model accuracy gate was marked DONE.
- Artifact: local ignored [source/docs ZIP](validation/artifacts/offline-source-with-docs-v3-2026-09-25.zip), 116 files, SHA256 `c196d5a5c47335b5cd3f7101c89835ec21d8a2bb92cf443d36ddd6929b287344`, passed ZIP integrity, extraction/README-link validation, Compose config and extracted-source Docker build. Image ID `sha256:bbda6ca0699c4a88c762121627e3caf282941bea8b3ea4d132d08f5b8fc0359a`. The ZIP predates this final receipt/handoff text, so it is a named checkpoint, not an exact snapshot of the later working tree. [R01 receipt](validation/r01-packaging-2026-09-24.md) has the scope and limitations.
- Isolated offline checks: that extracted-source image reached readiness on an internal-only network with Jev disabled, read-only local models and test database; outbound TCP returned `ENETUNREACH` 101. API/action/stream harness exited 0 with 22/22 groups in 40.667 s; three post-COMMIT ambiguity cases passed. The separate current-source repeated-public WAV timing check measured 5,216.10 ms median/5,341.72 ms p95 over 12 samples. Temporary validation backends/internal network were removed, while `vv-s02-db` stayed running/unpaused and the presentation stack was unchanged. No commit or push.
- Remaining: rights-cleared, source-disjoint four-language material or an explicit missed-gate deferral; model/policy freeze and untouched test only with suitable data; wire-level COMMIT and final-image recheck; final image/model archive and pristine-host offline/preserved-DB rehearsal; narrated approved final-build backup; three timed physical microphone/separate-verifier/projector/offline rehearsals and R04 sign-off. Keep the working local detector and fail-closed action path; do not turn fixture Jev results into service evidence.
- Documentation follow-up: the existing [offline package guide](validation/offline-package-2026-09-24.md) now explicitly warns that its `v03` image tar must not be mixed with the newer source/docs checkpoint. This later text is not in the hashed v3 ZIP, which remains a historical checkpoint rather than a current-tree release.
- Coordination note: Ruflo memory storage refused an unsafe write while a native WAL connection was active; no retry or database repair was attempted. This plan and its receipts remain the authoritative handoff.

### 2026-09-25 22:57 Asia/Calcutta | Codex-assisted partial rehearsal | R03

- Status: R03 moved from TODO to IN_PROGRESS, not DONE. `/readyz` was 200 before the user-operated rehearsal. The user reported ELEVATED and a simulated transfer completed after local verification, with no money moved. Read-only database checks found the newest session `FILE_READY`/ELEVATED with 1,744 ms speech and the newest action `COMPLETED`/ELEVATED 30.67 seconds after creation. Its challenge was VERIFIED, one approval was consumed and exactly one `ACTION_COMPLETED` audit event followed `ACTION_CREATED`, shadow/no-key `JEV_DECISION`, `OTP_CHALLENGE_ISSUED` and `OTP_VERIFIED`. No OTP, key, recipient or raw audio was read or recorded.
- Limits: the database proves the guarded local file/action path, not who operated `/verify`, how the physical microphone behaved, or whether Internet/projector were disconnected. The user was asked to confirm second-person and offline conditions; no answer to those points has been received yet. No new synthetic HIGH/BLOCKED action was observed in the latest action audit. Do not count this as one of the three full R03 rehearsals until those facts and the remaining cases are documented.
- Next: confirm the second-person verifier and Internet state; then run the known synthetic public-WAV HIGH/BLOCKED checkpoint and a fully disconnected timed rehearsal with the second person. Preserve the simulated-only claim and keep codes/keys private. No application code, model, threshold, Docker stack or database rows were changed by this read-only verification and plan handoff.

### 2026-09-25 23:40 Asia/Calcutta | Rehearsal clarification | R03

- The user confirmed that a second person personally operated `/verify` for the completed ELEVATED simulated transfer, and that the laptop's Internet was **not** disconnected. This confirms the staffed verifier role by user report, while the backend approval/audit result remains independently persisted. The connected run does not satisfy R03's offline condition or count as one of three full offline rehearsals.
- Next: rehearse the same guarded path with Internet disconnected, record live-mic and WAV risk/status separately, demonstrate the known synthetic HIGH/BLOCKED case, and note projector operation and timings. No code, model, threshold or runtime configuration was changed in this clarification.

### 2026-09-26 00:40 Asia/Calcutta | User-reported offline LOW run | R03

- User reported a full offline rehearsal that worked at LOW. Read-only database inspection found the latest session `FILE_READY`/LOW with 1,744 ms speech and the latest protected action COMPLETED/LOW 17.93 seconds after creation. Its challenge was VERIFIED, one approval was consumed, and its audit sequence was `ACTION_CREATED`, `JEV_DECISION`, `OTP_CHALLENGE_ISSUED`, `OTP_VERIFIED`, `ACTION_COMPLETED`. No key, code, payload or raw audio was read.
- The database supports the guarded file/action path, not Internet disconnection or physical/projector/role observations. Whether this run included a real-time microphone check, a separate verifier and a synthetic HIGH/BLOCKED demonstration is not yet stated; keep R03 IN_PROGRESS and do not count a full rehearsal until confirmed. The earlier connected ELEVATED run did have a user-confirmed second-person verifier. No app code, model, threshold or runtime setting was changed.
- Next: confirm those remaining conditions for this run, then complete the remaining timed offline rehearsals and final-source release checks.

### 2026-09-26 00:56 Asia/Calcutta | Offline rehearsal follow-up | R03

- The user answered yes that the offline LOW run included a physical live mic, second person on `/verify`, projector and synthetic HIGH/BLOCKED checkpoint. Record these as user-observed conditions. The local presentation DB independently confirms only the LOW completed action: its last HIGH evaluation was at 2026-09-25 07:07 UTC and last BLOCKED action was at 05:16 UTC, both well before the LOW run at 19:05 UTC. A later evaluation on the LOW run's session at 19:23 UTC was ELEVATED, not HIGH. This is an evidence mismatch, not proof the user did not observe a block; another stack/run or a UI-only observation is possible.
- R03 stays IN_PROGRESS. Do not count this as one of three *timed, fully evidenced* rehearsals until the synthetic action/audit timestamp and full-run elapsed time are reconciled. Ask only for nonsecret event/time details; never request a code, key, payload or private audio. No application code or runtime state changed by this read-only check.

### PDF source fingerprints

These bind the plan's source references; refresh them if the PDFs change.

| PDF | SHA256 |
|---|---|
| Context.pdf | `51aba5ac69e1bb29593a9964f5a40942d3beaae76cb74a61154bb81d7b2899e7` |
| SIH2026-IDEA-Presentation-Format final 2.pdf | `2b65f48bdf1344017d78fa085e03c048233def768fcc55cf0f77da0ca1d2944a` |
| docs/teacher-presentation.pdf | `1dd3a76b465d723f49787e68dc5a6be7f57f8b16b07a0b742d47b255d414929a` |
| docs/VigilVoice-Build-and-Launch-Guide.pdf | `7cf9e210c8e3bfa8b56ace13307d86c64dbb5d16b9177ea5ad1ea3dd5cba9dce` |
